{
  "name": "aproof-e2e",
  "private": true,
  "description": "Phase 0 closing gate: Playwright suite against the real docker stack",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
