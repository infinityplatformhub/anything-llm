# QA-1 evidence — #30 T-5 slice 3 @ `f77433728`

**Verdict: PASS** — no findings. My slice-2 NIT-1 (M8b pinned to a spelling) is closed.

Worktree `/tmp/qa1-s3` (detached, QA-owned, own `node_modules`). Probes `/tmp/ps3/`.
Node 22. §7.14 — probe + mutation on related files.

Baseline: **78/78** across 4 suites (`chat/index`, `rehydrationAcl`, `curateSourcesAclFields`,
`pinnedContextAcl`) plus **24/24** cardinality (`cardinalityHttp`, `cardinalityScope`).

## 1. S-22 — the filter runs before the push, at all five call sites

`fillSourceWindow` now requires an `aclFilter` and throws without one, and
`isRowAllowed(source, aclFilter)` is the **first** predicate inside the `validSources`
filter (`utils/helpers/chat/index.js:466`) — before `sources.push`, not at the return.

Brace-matched every call in the tree:

```
utils/chats/stream.js:232            OK
utils/chats/embed.js:174             OK
utils/chats/apiChatHandler.js:378    OK
utils/chats/apiChatHandler.js:785    OK
utils/telegramBot/chat/stream.js:272 OK
TOTAL call sites=5  missing aclFilter=0
```

Five, not four — `apiChatHandler.js` carries two (sync and stream). This is the shape that
made me count call sites by brace-matching rather than by file.

**Ordering is specifically constrained.** MB moves the filter to the return, keeping the
same predicate:

```
MB (filter moved AFTER push, at return) -> 4 failed
```

Three of those four are window-shape tests, which is the point: filtering after the push
lets denied sources consume window slots, so a denied actor silently receives *less*
context — and the shortfall is itself an oracle for how many citations they cannot read.

## 2. The reworked `chat/index.test.js` still exercises a real window

PMO's question. I drove the real `fillSourceWindow` with fixtures matching the suite's
shapes and reported what came back:

| case | sources | contextTexts | ids |
|---|---|---|---|
| backfill 1 from history | 3 | 3 | a, b, c |
| window already full | 1 | 1 | a |
| history empty | 1 | 1 | a |
| all history denied (ws2) | 1 | 1 | a |
| mixed allow/deny | 2 | 2 | a, **c** |

Backfill genuinely happens (1 → 3), so the ACL fields added to those 16 call sites did not
turn the suite into "an empty window passes everything". `contextTexts.length` equals
`sources.length` in every case, and the mixed case admits `c` while excluding the denied
`b` — the two outputs agree, which is the property that matters since `contextTexts` is what
reaches the LLM.

## 3. Mutation — rehydration and the predicate

| # | mutation | result |
|---|---|---|
| MA | `isRowAllowed(...)` → `true` (no ACL on rehydration) | **8 failed** |
| MB | filter moved after push | **4 failed** |
| MC | unlabelled source always allowed (PMO's named mutation) | **1 failed** |
| MD | cross-org check dropped | **11 failed** |
| ME | cross-workspace scope dropped | **11 failed** |

MC is the "source with no label → unprovable" case PMO asked for: it dies on
*"a citation stored before slice 1a…"*, so the `RETRIEVAL_FILTER_ALLOW_UNPROVABLE` behaviour
is pinned rather than assumed.

MD and ME each take the ten-provider `curateSourcesAclFields` table down with them, which
confirms that table is judging the real predicate rather than asserting field presence.

## 4. `curateSources` — ten providers, not eight

The table covers `astra, chroma, chromacloud, lance, milvus, pgvector, pinecone, qdrant,
weaviate, zilliz`. TL-2's pre-review measured eight and omitted `chromacloud` and `zilliz`,
which I flagged in pre-read; both are present here, each with its own `curateSources`
override rather than an inherited one.

## 5. M8b — now behavioural, and it catches what the grep cannot

My slice-2 NIT-1 was that M8b greps `pinnedContext.js` for `action: "document.read"`, so a
refactor keeping the string could change the behaviour undetected. Slice 3 adds a
behavioural test that drives the bridge against an ALLOW-search / DENY-read document, with a
real user row as a positive control.

Proven by a mutation designed to defeat the grep — the action is changed to
`document.search` via a variable while the literal `"document.read"` stays in the file:

```
MF (action changed, literal kept) -> 1 failed
  "the pinned bridge picks document.read by BEHAVIOUR"
```

The grep-based M8b passes under MF; the behavioural test does not. Both are kept, and they
catch different failures.

## 6. S-25 — cardinality

`scopedNamespaceCount` refuses **before** touching the vector store, and returns `null`
for all three of: match-none actor, workspace that does not exist, workspace outside scope.
The route turns `null` into 404, so "not yours" and "not there" are the same answer.

| # | mutation | result |
|---|---|---|
| MG | slug scope check dropped (counts any workspace) | **1 failed** |
| MH | match-none actor gets a count | **1 failed** |
| MI | out-of-scope throws instead of returning `null` | **1 failed** |

MI is the distinguishability case: an exception surfaces differently from a 404, and the
suite catches it.

`cardinalityHttp.test.js:234-243` compares an absent workspace against an out-of-scope one
on **status, body text and content-type**, and asserts the answer is not
200-with-empty-results — existence must not be confirmable by shape. It carries a positive
control (the member's own workspace is reachable), so the matching refusals are not two
identical failures for the wrong reason.
