# QA-1 — #49 2c9ae1f0f — PASS (1 NIT)

worktree /tmp/qa1-49, own deps + prisma generate after rebase. Baseline 5 suites 86/86.

Middleware removal, disjoint red sets: M1 embedSessionOpen 84/86 (origin, disabled); M2 embedHistoryRateLimit 82/86; M3 validEmbedConfig 69/86; M4 requireSessionToken→false 81/86. Sweep exemption 316→317 is honest — every gate removal is red.

Token scheme: M5 rotation resets firstIssuedAt → red; M6 remove 7d cap → red; M7 back to join("|") → 4 red; M8 route accepts body.sessionId → red; M9 rotation skips verify → 7 red.

Oracle rows/no-rows byte-identical (text + content-length). Cross-IP via socket.remoteAddress override, IPv4 ×2, first IP still 429 after second served.

NIT-1 (test gap): the two skew bounds (`issuedAt > now+skew`, `firstIssuedAt > now+skew`, embedSessionToken.js:206-209) each survive alone (M10, M11 86/86; M12 both → 2 red). Measured non-equivalent: M10 accepts issuedAt=now+1y with firstIssuedAt=now-1s. Cause: every F2 test moves both stamps together. Fix: 2 cross-stamp tests. Not exploitable at this SHA (openSession stamps equal; mintIfEntitled reads firstIssuedAt from a verified token). `Number.isSafeInteger` "redundant" comment holds only while both bounds are pinned.
