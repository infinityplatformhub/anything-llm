# QA-2 — #136 S12 slice 1 offboarding — 49f7a0cd9 — PASS (1 NIT → became TL-2 blocker ruling; 3 suite gaps)
Written by PMO from QA-2's verdict body (QA-2 is read-only). Worktree /tmp/qa2-136, fresh DB, real HTTP.

Retraction: POST /admin/generate-api-key mints for the SESSION user (userFromSession; body userId ignored) — cannot target a suspended user (probe C 6/6). Earlier claim was model-level reachability reported as HTTP. Re-suspend no-op has no HTTP-reachable consequence in this slice.

Standing finding: api-key suspension enforced only by the sweep in User.update; resolveActor api-key branch → keyGrantPrincipal(createdBy) never reads users.suspended. users.suspended=1 without sweep → 200; User._update(id,{suspended:1}) → revokedAt null, 200 (exported, live callers PasswordRecovery:25,91, PushNotifications:152). Extension key safe at reader; orphaned key refused in multi-user. Recommendation: reader-side check (adopted by TL-2 ruling 0fa6713ba).

Targets (probe A 27/27): both key kinds 403 after / 200 before; $use throw on api_keys.updateMany → success:false, suspended rolls back, still authenticates; DELETE member: unauth + legacy-default refused with row surviving, admin 200 row gone + policy_versions group_membership + policy.changed outbox (counted by type); isConfirmedSingleUser false at 1 and many, true at 0.

Mutations (probe / dev suite): M1 drop revoke 5/2 · M2 revoke outside tx 2/1 · M3 gate→validatedRequest 6/1 · M4b removeGroupMember→direct delete 3/0 GAP · M5 drop revokedAt:null filter 1/0 GAP · M6 drop createdBy scoping 1/0 GAP · M7 isSuspending ignores prior 1/0 (behaviour) · M8 isConfirmedSingleUser ≤1 —/1.

Suite gaps: (M4b) happy path asserts 200 + row count only — add policy_versions bump + policy.changed outbox counted BY TYPE (uuids not chronological; auth.key_used fires concurrently); (M5) already-revoked key must keep original revokedAt; (M6) blast radius — revoking without createdBy filter must fail.
