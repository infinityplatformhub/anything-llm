# QA-2 evidence — #30 slice 1a b35c73eb — PASS 62/62 (129 suites / 1332 tests)

Author: QA-2 (anything-llm-e6), transcribed by PMO. Pinned detached worktree, node 22.23.1, PG16 own container port 5497.

## 1. `toJsonbSql` bind() — 27/27
G1 every shape × both flag states: placeholders contiguous `$3,$4,$5…`, none reused, count = params.length, `$3` referenced. G2 real pg per shape × state, rows checked (not just "ran"). G6 RED: reconstructed old `next()` still rejected. G9 `startIndex` honoured. G3 orgWide no cross-org rows; G4 deny-list cuts (positive control present); G5 docId-less refused with deny-list both states; G7 docId bound; G8 matchNone → null.
Note: QA-2's first orgWide expectation was wrong (org-wide grant legitimately spans all workspaces in the org); Dev4 correct.

## 2. FINDING-1 legacy table — 12/12
L1 `hasAclColumns`: legacy=false, modern=true, nulled-column=true; L4 same outcome per state. L1b/L1c RED: strict predicate and `IS NULL` escape both throw on legacy table. L1d unreadable schema → labelled (strict) path. L1e 2-of-3 columns → legacy. L2 flag OFF → empty + reason logged. L3 flag ON → serves old rows. L3b/L3c not a bypass: matchNone still empty, org2 rows cut by isRowAllowed. L6 rerank stays within topN.

## 3. FINDING-2 `unprovableVectorCount` — 5/5
N1 real lance dir → `{unlabelled:3,total:4}` (2 legacy + 1 null). N2 milvus → `{unsupported:true}`. N3 real failure → `{error}` with message. N4 error → logger.error, unsupported → logger.warn, levels distinct. N5 backticked countRows.

## 4. Original 6 criteria re-run — 18/18, no regression.

Verdict: PASS, no fix list.
