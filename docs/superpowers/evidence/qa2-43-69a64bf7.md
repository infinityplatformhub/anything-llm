# QA-2 evidence — #43 69a64bf7 (S2 SAML full) — PASS 40/40 · 138/138 suites, 1446/1446

Author: QA-2 (anything-llm-e6), transcribed by PMO. Pinned SHA, node 22.23.1, pepper 43 bytes, PG16 own container :5499. One suite red = #57 flake (`--runInBand` 20/20).

1. e2e + replay (4/4): real assertion → 200 + token opens an authenticated route; replay → 401, still 1 row; new RelayState + same assertion → refused; old RelayState + new assertion → refused.
2. refuse-without-claim (12/12): wrongRecipient/wrongIssuer/expired/wrongAudience/unsigned/wrongKey/xswWrapped/xswSibling/xswHidden/selfSignedWithKeyInfo/wrongInResponseTo → 401 and zero rows (total count before/after); xswUnwrappedSubject creates no attacker identity.
3. burn attack (2/2): broken-signature doc with victim ID → 401, claims=0; victim's real assertion still 200.
4. mount order (2/2): /sso/saml/login → 302 to IdP with RelayState; bogus provider → 404.
5. ACS URL from config (2/2): forged Host matching forged Recipient → 401; forged Host + real assertion → 200.
6. rate limit (2/2): /acs and /login → 429. 7. no oracle (3/3): 6 failure modes → byte-identical body; no StatusCode/urn leak; malformed body no parser detail.
8. Host-fallback warning (9/9): fires every boot [1,1,1]; either URL var silences; SAML off silent; all truthy spellings; warnIfMisconfigured lists only missing vars; plural SSO_SAML_CERTIFICATES accepted.
9. cross-provider state (4/4): SAML state at OIDC callback refused; OIDC state at SAML ACS → 401, claims=0; refusal byte-identical; positive control 200.
Mutations 8/8 caught (remove _checkRecipient 3, claim-before-check 8, cross-provider 2, ACS from Host 8, _checkIssuer 2, _checkAudience 2, _checkConditions 2, rate limiter 1).

Probe self-corrections (recorded per §7.9): editing Recipient in a signed doc breaks the signature, so the refusal came from signature check not _checkRecipient (mutation exposed it) → use the fixture that signs the wrong Recipient; ACS_URL first match was Destination not Recipient; xml-crypto emits SignatureValue without `ds:` prefix; forged Host must equal forged Recipient origin; reset rate-limit controls per case.

Verdict: PASS, no fix list.
