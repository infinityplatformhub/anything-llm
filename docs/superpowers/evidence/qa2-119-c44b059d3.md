# QA-2 — #119 `c44b059d3` — FAIL (recorded by PMO from QA-2 message body; QA-2 is read-only)

Probe 41 cases: 40 green, 1 red. Dev suite routeMountGuard 38/38.

## FINDING: `app._router` is not sealed
`sealRoutes(app, apiRouter)` seals the two objects passed; express delegates to `app._router`, a different object. Measured on the shipped app after seal: `app.post` THROWS; `app._router.post`, `.all`, `.route().post`, `.use(router)` all MOUNTED. HTTP proof (NODE_ENV=production): layer spliced before the terminal `app.all("*")` → `POST /qa2-internal` 200 with handler marker. Position matters: a plain `push` lands after the catch-all and looks refused (200, no marker); splice@7..14 hit, @15 miss. Anyone with `_router` can write the same array.
Severity: like the accepted captured-before-seal residual but needs no pre-boot preparation.
Options: (1) `sealRoutes(app, app._router, apiRouter)` with a guard for a not-yet-created `_router`; (2) declare residual with a test asserting it DOES mount.

## Everything #119 targeted is closed
Marker oracle (not status; unmounted path returns 200 with no marker). Refused and unreachable over HTTP: pre-populated Router, empty Router filled later, `express()` sub-app, empty sub-app filled later, nested 2 levels, `.route(p).post()`, all 6 SEALED_METHODS. Middleware (json/urlencoded/static/text/plain fn) mounts and runs.
Extra shapes: fn with `.use` but not a router → passes (correct); `router.param` not sealed (observed); `app.route()` refused. `router.get`/`router.head` THROW (Router uses `.route` internally) while `app.get` passes — comment claiming get/head are exempt is true only for `app`. `nestedSealables` survives a cycle.
Mutations 4/4 caught: seal-no-throw 13/8, throw-no-seal 10/6, `.stack` 5/2 (both directions), GUARD=off-still-seals 2/2 (with HTTP control that late mount serves when off). ROUTE_MOUNT_GUARD exact-value: only lowercase `off` disables.
