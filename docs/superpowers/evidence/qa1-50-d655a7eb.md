# QA-1 evidence — #50 5c05a935 / d655a7eb — PASS 6/6 (gate 1546/1546, no flakes)

Author: QA-1 (anything-llm-af), transcribed by PMO. Own worktree, yarn install; d655a7eb verified comment-only (migration SQL md5 identical after stripping comments).

1. Removed routes → 404 for none/admin JWT/user.read key/empty-scope key; only OIDC login/callback remain; ROUTE_SCOPES has no sso.issue.
2. Migration from a DB holding sso.issue: key ["sso.issue","user.read"] → ["user.read"] (still 200 on user.read route); key with sso.issue only → [] (403 everywhere, still resolves, not revoked); permission + role_permissions rows gone; policy_versions +1; NOTICE lists keyPrefix; manual re-run no-op; migrate deploy idempotent.
3. NO_LOGIN on shape (b): /invite 403 (middleware site), /request-token 401 via #58 guard (no token either way); control without flag → 200.
4. Unauthenticated payload: SSOProviders ids only; no issuer/clientId/secret.
5. OIDC single-use: helper replay/expired/unknown refused, 3-way concurrent → 1 winner; HTTP replayed/unknown/expired → byte-identical 401; TemporaryAuthToken second redeem refused.
6. SAML in SSOProviders: registry-derived; all truthy spellings; no entryPoint/cert/issuer leak.
Mutations 4/4 caught (raw NO_LOGIN, drop SSOProviders, ignore ENABLED, re-seed sso.issue).
