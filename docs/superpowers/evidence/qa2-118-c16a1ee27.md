# QA-2 — #118 c16a1ee27 — PASS

Probe 31/31 via AuditEventSubscriber → event_logs; dev suite 169/169. Fixtures: fullwidth phone leading ０; mixed ASCII/fullwidth 13/17 and fullwidth glued to 13-ASCII → long_digit_run (proves digit-class lookaround); mod-11 valid/invalid both thai_national_id, Luhn-invalid still credit_card (checksums computed in test); U+3000/U+FF0D separators not redacted with ASCII-space control (residual #120).

| mutant | probe | dev |
|---|---|---|
| M1 remove long_digit_run | 6 | 4 |
| M2 ASCII-only class | 12 | 4 |
| M3 move long_digit_run first | 0 | 0 — equivalent, exhaustive 247 cases (1–40 digits × 6 shapes) 0 differences |

Confirmed thai_national_id-before-credit_card IS load-bearing (swap → 13-digit ids labelled credit_card). Boundaries 16/17 exact; no residual \d{4} beside markers; 12 fullwidth not matched; wide() helper guarded.
