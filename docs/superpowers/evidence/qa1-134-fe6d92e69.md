# QA-1 — #134 S4b slice 2 apply + checkpoint — fe6d92e69 — PASS (1 NIT)
Written by PMO from QA-1's verdict body (QA-1 is read-only). Tree /tmp/qa1-134, DB qa1_134 fresh (3 migrations + seed). Baseline 37/37.

1. Membership write goes through the repository: 1 user + 1 group + 1 addMembership ⇒ checkpoint completed, policy_versions +1, group_members 1. Mutant MA (direct group_members.createMany) red on 2 tests.
2. Refused plan applies nothing: create + createGroups + addMembership all 0; newUsers/newGroups/newMembers 0.
3. CHECK constraints via Prisma client both directions: bogus status REJECTED; refused+NULL reason REJECTED; completed+reason REJECTED; failed+reason REJECTED; refused+reason ACCEPTED; completed+NULL ACCEPTED. Observation: nothing writes status 'failed'; a crash leaves no row (deliberate) — residual for slice 3: "no row" ≠ "never ran".
4. Rollback scope, injected throw on entry 2 of 3 removals: entry 1 persisted, checkpoint rows 0 — documented R4/R5 behaviour. Mutants: MA red (35/37); MB (whole loop in one $transaction) SURVIVES 37/37 with policy_versions +3, outbox +3 — identical to clean. The comment's predicted collapse-to-one-bump does not occur for this shape; neither RF-2 counts nor a conflict fixture witness R4 as written.
   NIT-1: R4 stated at length, unpinned. Option 1 find the mutant that reaches the hazard; option 2 soften the comment to what is true (db passed so each call opens its own tx; risk is a future refactor batching bumps).
5. Idempotency: same plan twice ⇒ 0 new rows, group_members still 1. Caveat: membershipsAdded reports 1 on a no-op re-apply and version still bumps (counter per call, not per change) — slice 3 operator view should say "entries applied". Dangling membership (group absent) skipped, no NaN write.
