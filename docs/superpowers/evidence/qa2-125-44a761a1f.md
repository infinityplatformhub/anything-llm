# QA-2 — #125 `44a761a1f` — PASS (recorded by PMO from QA-2 message body; QA-2 is read-only)

Probe 24/24 + dev suite 25/25 = 49/49.

Mutations: M1 remove cache read → 9 red (probe 4, dev 5). M2 keyed by nothing → 4 red. M3 guard below cache → 0 red, **proven equivalent structurally**: cache stores only guard-passed material; `===` compares string value; JS strings immutable; `process.env` coerces to string on write. So `cache.material === m` ⟹ m passed guard. No input distinguishes the versions. Dev5's "guard before cache, deliberately" comment stays as intent (protects future cache-key changes).

Extra probes green: SIG_KEY rotate GOOD→OTHER→GOOD returns original value, 3 derivations (keyed-by-nothing would give 1). Boundary: exactly 32 PASS; 31 THROW; 32+trailing space PASS; 32 spaces THROW; 31+space THROW; leading space+32 PASS (guard reads `trim().length`). Concurrency: 20 `get()` cold → 1 derivation. Side channel closed: present vs absent after warm → 0 derivations both. Key not reachable via `Object.keys`/`JSON.stringify`/exports, no 64-hex in console.error on decrypt failure or SIG_KEY refusal. AAD still binds envKey; old keyVersion → null.

Derivation count via `crypto.scryptSync` spy, not clock.
