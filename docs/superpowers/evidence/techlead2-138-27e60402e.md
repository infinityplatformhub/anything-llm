# Techlead-2 — #138 combined `27e60402e`: does the `4c01ac1ca` verdict carry?

**Skills invoked:** `security-review` (auth tier). `requesting-code-review` does not resolve
by name in this session (`Unknown skill`, bare and `superpowers:`-namespaced), so the
reviewer template was read from disk. No `infi-lessons` line.

**Answer: it carries — and I re-fired anyway rather than reasoning from the diff.**
My PASS on the queue/apply side (`8dbff801d`, corrected by `95c115d4b`) stands on
`27e60402e` unchanged.

---

## Why re-firing was the right call even though the diff says it was unnecessary

`git diff 4c01ac1ca 27e60402e` over the three files my verdict rests on —
`utils/identity/applyDirectoryPlan.js`, `utils/jobs/`, and
`__tests__/security/jobs/directorySyncConcurrency.test.js` — is **empty**. So the code and
the fixtures under my verdict are byte-identical.

But the combined SHA adds a migration, changes `prisma/seed.js` and
`prisma/seeds/permissions.js`, and introduces the `directory.sync` permission. A verdict
that depends on fixtures which read the seeded policy tables is not insulated from a seed
change by an empty source diff. Re-firing costs one run; assuming costs a wrong PASS.

## Measured on `27e60402e`

Worktree `/tmp/tl2-138c`, database built for this SHA rather than cloned from the old
template — `prisma db push` + `node prisma/seed.js`, then verified the new objects exist
before trusting any result:

```
SELECT count(*) FROM permissions WHERE action='directory.sync'  -> 1
SELECT to_regclass('directory_sync_checkpoints')                -> directory_sync_checkpoints
```

Both are new at this SHA and absent from `t98b`, which is why the old scratch template
could not have been reused. A seeded template was then cloned per mutant run.

**Baseline: 20 passed, 20 total.**

| mutation (fired by BEHAVIOUR, named after) | result |
|---|---|
| `if (true)` — guard refuses everyone | RF-7 red at `pausedAfter` `Expected: 2 / Received: 0` (setup-reason) **and RF-7b red** — 2 failed / 20 |
| `if (false)` — guard admits everyone | RF-7 red at `Expected constructor: LeaseLostError / Received constructor: Object`, **RF-7b green** — 1 failed / 20 |
| heartbeat drops `leaseUntil: {gt: now}` | **RF-8 red** (`Received promise resolved instead of rejected`) — 1 failed / 20 |

Identical to `4c01ac1ca` in every particular, including which assertion fails and with what
message. The discriminator finding is unchanged: admits-everyone is caught by RF-7 alone
with RF-7b green, refuses-everyone is caught by RF-7b, neither subsumes the other. My M3
NIT stays closed by RF-8.

Mutants were fired by behaviour and labelled afterwards — the practice I got wrong on
`8dbff801d` and corrected in `95c115d4b`.

## Verdict

**PASS carries to `27e60402e`.** Queue/apply side only; the permission side is TL-1's.

## Reproduction

```
git worktree add --detach /tmp/tl2-138c 27e60402e
cp -al /tmp/tl2-138v/server/node_modules /tmp/tl2-138c/server/node_modules
cd /tmp/tl2-138c/server && npx prisma generate
createdb t138c && npx prisma db push --skip-generate --accept-data-loss && node prisma/seed.js
psql -c "CREATE DATABASE t138c_tmpl TEMPLATE t138c"      # seeded template
# before each mutant run: DROP t138c; CREATE t138c TEMPLATE t138c_tmpl
npx jest __tests__/security/jobs/directorySyncConcurrency.test.js --runInBand
```

A database cloned from `t98b` will NOT work at this SHA: it has neither the
`directory.sync` permission row nor `directory_sync_checkpoints`.
