# QA-1 evidence — #48 06965da4 — FAIL (BLOCKER-1)

Author: QA-1 (anything-llm-af), transcribed by PMO. Own detached worktree /tmp/qa1-48, gate 1378/1378 (134 suites).

## BLOCKER-1 — `DELETE /system/credential/AUTH_TOKEN` unlocks the instance
`updateENV.js:1846` accepts every KEY_MAPPING `secret:true` key (91). Two are instance auth, not provider credentials: `JWT_SECRET`, `AUTH_TOKEN`. `validatedRequest.js:29-36` calls `next()` without credentials when either env is unset.
```
E0 login with env AUTH_TOKEN            -> 200
E1 POST /system/update-password         -> 200
E2 AUTH_TOKEN + JWT_SECRET rows stored  -> yes
E4 unauth POST /system/update-env       -> 401
E5 DELETE /system/credential/AUTH_TOKEN -> 200 cleared, env UNSET
E6 unauth POST /system/update-env       -> 200   (open; permanent — row gone, boot cannot restore)
J1 clear JWT_SECRET -> 200; J3 existing admin JWT -> 401 (all sessions dead)
```
Side effect: `/request-token` 500 (`bcrypt.hashSync(undefined)` system.js:376) instead of 401.
Why untested: `credentialClearHttp.test.js` only clears `OPEN_AI_KEY`; "which keys are clearable" has no test.

## 7 ordered checks — all PASS
(1) session-only settings.write: no token 401, moderator 403, super_admin 200; only `DELETE /system/credential/:envKey` matches; `/v1/...` with API key 404. (2) row before env: store.delete false → cleared:false, env intact, row intact; store throw → 400 env intact. (3) STORAGE_DIR/DATABASE_URL/SIG_KEY/SIG_SALT/PATH/NODE_ENV/API_KEY_PEPPER → 400 untouched; JWT_SECRET → 400 "could not be removed" = passed allowlist (BLOCKER-1). (4) valid key no row → 400. (5) `loadStoredCredentials()` after clear → stays unset. (6) audit `{"envKey":"OPEN_AI_KEY"}` only; no secret/JWT/SIG/PEPPER leak; free text / path traversal / trailing space / `*` / lowercase → 400, no audit row. (7) #41 NIT-1 test catches mutation.

## Mutations (baseline 49/49): M1–M6 all caught (null Set, env-before-row, drop allowlist, row-without-env, drop requirePermission, drop ALLOWED_KEYS envKey).

## NIT-1 (non-blocking)
`/request-token` 500 when AUTH_TOKEN undefined; state reachable via `update-password usePassword:false` too. Should be 401.
