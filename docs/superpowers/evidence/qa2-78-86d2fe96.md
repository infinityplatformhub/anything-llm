# QA-2 — #78 `86d2fe96` — PASS

Probe 20/20 · mutation 10/10 · ไม่รัน full suite (§7.14) · เขียนโดย PMO จากข้อความ QA-2

## รอยต่อกับ #72 — ลำดับ forbidden(403) → protected(400) → unknown(400)
| caller | key | ผล |
|---|---|---|
| manager | forbidden (`text_splitter_chunk_size`) | 403 `forbidden_keys` เขียน 0 แถว |
| manager | unknown | 400 `unknown_keys` (ไม่ใช่ 403 — ไม่ใช่สิทธิ์ และ 403 จะเป็น oracle) |
| manager | protected (`multi_user_mode`) | 403 (union) |
| manager | forbidden + unknown | 403 ชนะ |
| admin | protected | 400 `protected_keys` — #72 ไม่ถูก revert |
| admin | unknown | 400 `unknown_keys` — #72 ไม่ถูก revert |
CONTROL: manager + allowed → 200 เขียนจริง · admin + manager-forbidden → 200 เขียนจริง (engine จริง)

## 4 route
`upload-logo` / `remove-logo` / `toggle-live-sync` ปฏิเสธ manager + DB ไม่เปลี่ยน · `enable-multi-user` gate `system.write` (source assert + behaviour)

## narrowing passthrough
loop copy ทุก key ไม่ filter — allowed copy verbatim, object ผู้เรียกไม่ mutate, unknown รอดไปให้ #72 ตอบ 400

## Mutation 10/10
forbidden guard · union ตัด protectedFields · filter แทน passthrough · system.write bypass · ถอด narrowing admin/community-hub/live-sync ทีละตัว · enable-multi-user กลับ settings.write · managerAllowedFields ไม่ freeze
M6 รอดรอบแรก: เคส community-hub ใช้ admin ทั้งหมด (ผ่าน narrowing อยู่แล้ว) → เพิ่ม manager เขียน `hub_api_key` → 403 + CONTROL admin เขียนได้ · บทเรียน: actor ผิดคน / fixture ค่าศูนย์ ดูเหมือนทดสอบ guard แต่ไม่แตะ

## drift test
assertion แรก self-satisfying แก้เป็น: ไฟล์ที่เรียก `updateSystemPreferences` ใน `GeneralSettings/Settings/components` ต้องอยู่ใน list · `managerAllowedFields` frozen ยืนยัน
