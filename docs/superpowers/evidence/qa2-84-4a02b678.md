# QA-2 — #84 `4a02b678` — PASS (update-env + credential delete; carries to f309f1247 = test-only +29)

Probe 17/17 · mutation 5/5 · ไม่รัน full suite (§7.14) · เขียนโดย PMO จากข้อความ QA-2

## สองประตู
`system.js:731` POST /system/update-env → system.write · `:740` DELETE /system/credential/:envKey → system.write · บน 8bcc62ce manager ลบได้ 200 (FINDING-1 ยืนยันด้วยการยิง)

## เคส 17
premise guard 4 · manager write refused 4 (secret/non-secret/สุ่ม 3/junk) · manager delete refused 3 (403 + credential รอดใน store / admin ลบจริง / gate-equality เทียบสองบรรทัด ไม่ hardcode) · admin control 3 (non-secret เปลี่ยน / secret persist / `****` ข้าม) · /v1 3

## Mutation 5/5
update-env gate ย้อน (5) · DELETE gate ย้อน (2) · DELETE ถอด requirePermission (2) · ถอด placeholder filter (1) · /v1 drift (2)

## ตัวเลข 214 / 92 (module load) · residual → #91
