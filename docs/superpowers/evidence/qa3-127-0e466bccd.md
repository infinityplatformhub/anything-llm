# QA-3 — #127 re-probe `0e466bccd` (Dev4, tier `plain`)

**Verdict: PASS.** Both findings from `610632d48` are closed, and neither fix cost coverage
elsewhere. Test-only diff (+11/−3 frontend, +5/−1 server); production code untouched since
`2ba5ca93a`, which is the right shape for a coverage fix.

Worktree `/tmp/qa3-127` @ `0e466bccd`. Node 22. Database `qa3_127` (fresh migrate + seed,
rebuilt after the earlier destructive probes).

## Baselines — exit codes

| | result |
|---|---|
| `yarn test` (frontend) | **exit 0**, 98 passed |
| `npx jest systemReadGrantDrift --runInBand` | **exit 0**, 4 passed |

## 1. F1 no longer fails open

The fix asserts both offsets before slicing:

```js
const routeStart = source.indexOf('path: "/settings/mobile-connections"');
expect(routeStart).toBeGreaterThan(0);
const routeEnd = block.indexOf("},\n      {");
expect(routeEnd).toBeGreaterThan(0);
```

| # | mutation | `610632d48` | `0e466bccd` |
|---|---|---|---|
| **P-bleed** | route ungated (`<MobileConnections />`), delimiter broken, `// AdminRoute Component={MobileConnections}` planted 60 lines below | **PASSED (exit 0)** | **killed** — exit 1, F1 fails |
| P-fp | route **correct**, delimiter broken | passed | **now fails** — exit 1 |
| D1 | `main.jsx` reverted to `2ba5ca93a^` (route back to `ManagerRoute`) | killed | **killed**, exit 1 |
| P-ungated | route uses no guard, delimiter intact | killed | **killed**, exit 1 |

P-fp changing from pass to fail is the intended cost and the correct direction: a broken
delimiter now **fails closed** — the guard refuses to vouch for a file it can no longer
parse, instead of silently widening. A prettier run or route reorder now produces a red
test naming the route, which is a five-second fix, rather than an assertion that quietly
stops checking anything.

## 2. Drift test counts both routes

```js
expect(gated.filter((action) => action === "system.read")).toHaveLength(2);
```

| # | mutation | `610632d48` | `0e466bccd` |
|---|---|---|---|
| **R2** | re-gate **one of the two** mobile routes `system.read` → `system.write` | **SURVIVED** | **killed** — exit 1, `the routes this protects still ask for system.read` |
| R1 | re-gate **both** | killed | **killed**, exit 1 |
| R2b | plant a **third** `requirePermission("system.read")` in the file | (n/a) | **killed**, exit 1 |

R2b was not asked for and is worth noting: the count is exact, so it also catches drift in
the *other* direction — a new `system.read` route appearing in this file breaks the pairing
claim and says so. That is stricter than the comment promises; it is the right strictness
here (the comment names "the routes this protects", plural and specific), but whoever adds
a third route to `endpoints/mobile/index.js` will need to update the count deliberately,
which is the point.

## 3. Nothing else regressed

| # | mutation | result |
|---|---|---|
| G1 | `AdminRoute` asks `user.manage` instead of `settings.write` | **killed**, exit 1 — `a manager is refused by AdminRoute` |
| G2 | `AdminRoute` drops `\|\| !multiUserMode` | **killed**, exit 1 — `single-user mode admits everyone…` |
| F7 | `INSERT` `system.read` for `member:org` in the real database | **killed**, exit 1 — `exactly one role holds system.read` |
| F7-revert | `DELETE` that row | **exit 0**, 4 passed; holders back to `super_admin:org` alone |

G3 (AdminRoute's `capabilitiesLoading` guard) still survives and is still **out of this
diff** — it is #40 t4's line at `PrivateRoute/index.jsx:103`. Unchanged residual, not a
#127 finding.

## Housekeeping

`git status --porcelain` clean in `/tmp/qa3-127`. Database verified back to one holder,
`super_admin:org`. No commits.

Supersedes `qa3-127-610632d48.md` (FAIL).
