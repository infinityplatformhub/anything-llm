# QA-2 — #112 43cf5da87 — PASS (auth half)

Probe 30/30 + dev preflightHttp 9/9. A positive control; B gate: pre-user 200, transition closes in-process and reopens when users deleted (no latch), multi-user mode closes route even with 0 user rows, system.read → refused no checks, same principal + system.write → 200, anonymous/forged refused, exposure differs from /onboarding in same state; C redaction: dotless/dotted hosts, 8-char partial step 2, pg prose ×3, remedy scrubbed; D shape derived from CHECK_IDS; E body keys === ["checks"]; F DB down: user present → refused, fresh instance → refused (pinned residual), admin still gets checks.

| mutant | probe | dev |
|---|---|---|
| M1 remove user guard | 9 | 4 |
| M2 system.write→read | 1 | 1 |
| M3 remove scrubText | 10 | 2 |
| M4 scrubValue instead | 9 (incl. dotted) | 2 |
| M5 catch fails open | 0 | 0 — inert: isConfirmedSingleUser never throws (own try/catch actorResolver.js:318) |

NIT: preUserOrGated catch block unreachable (inert-guard shape, cf. #80/#40). Not blocking.
