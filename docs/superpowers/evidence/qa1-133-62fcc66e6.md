# QA-1 — #133 S4b slice 1, the pure directory diff — `62fcc66e6`

Verdict: **PASS** (1 NIT — the cross-clear is unpinned; 2 carried NITs from `f0d016be0` remain)

Tree: `/tmp/qa1-133b`, own `node_modules`. `directoryDiff.test.js` baseline **21/21**.

## The two holes I raised at `f0d016be0` are closed — re-measured, not re-read

Same probe scripts, same fixtures, run against the new code:

```
RF-6  100 users all present, groupExternalIds [] for everyone
      before : deactivate 0 · refused false · removeMembership 100/100
      after  : deactivate 0 · refused TRUE  · removeMembership 0

RF-7  quarantined subject (email null) holding a membership
      before : quarantine [q0] · deactivate [] · removeMembership [{q0,g1}]
      after  : quarantine [q0] · deactivate [] · removeMembership []
```

## Membership-guard boundaries, measured

`MEMBERSHIP_FLOOR = 25`, `MEMBERSHIP_RATIO = 0.5`, both compared with strict `>`:

```
25 of 100  (== FLOOR, ratio .25)   allowed
26 of 100  (above floor, ratio .26) allowed      ← ratio arm doing the work
30 of 100  (above floor, ratio .30) allowed      ← the NIT-1 fixture's band
20 of 30   (below floor, ratio .67) allowed      ← floor arm doing the work
25 of 40   (== FLOOR, ratio .625)   allowed      ← strict `>` on the floor
26 of 40   (above both)             REFUSED
50 of 100  (ratio == .5 exactly)    allowed      ← strict `>` on the ratio
51 of 100  (above both)             REFUSED
```

No off-by-one on either arm, and consistent with the deactivation guard's own strictness (`10 of 18` allowed, `11 of 18` refused, `50 of 100` allowed, `51 of 100` refused — re-measured, unchanged).

## Mutations

| # | mutation | result | red |
|---|---|---|---|
| D2m | drop the MEMBERSHIP ratio arm (`… && true`) | 20/21 | the new NIT-1 test |
| DQ | remove the quarantine filter from `removeMembership` | 19/21 | RF-7 ×2 |
| DC | `refused` no longer clears `removeMembership` | 20/21 | RF-6 everyone-present |
| DF | `MEMBERSHIP_FLOOR` 25 → 0 | 19/21 | RF-6 small-org + R5 replay |
| D7 | removal ignores `complete` | 19/21 | T1 interrupted + T3 listGroups |
| DX | membership guard clears only its own list | **21/21 SURVIVES** | — |
| D2 | drop the DEACTIVATION ratio arm | **21/21 SURVIVES** | — |

D2m, DQ, DC, DF and D7 all go red on distinct sets. **D7 is now killed on two tests** — that closes my NIT-2 from `f0d016be0`, and RF-7's twin (`a NON-quarantined user in the same run still loses the membership`) is the positive control that keeps DQ from being satisfied by "never remove anything".

## NIT-1 (new) — the cross-clear is stated but unpinned

The return comment says both lists are cleared "whichever guard fired, since a run this wrong about one is not to be trusted about the other". DX weakens `deactivate: refused ? []` to `refusedDeactivations ? []` and the suite stays **21/21**.

Measured, on a run where the membership guard fires while deactivations are non-empty (100 users, 12 depart → ratio .12, under the deactivation threshold; the 88 who remain report no departments → 88 removals, ratio .88):

```
clean tree : refused true · deactivate 0  · removeMembership 0
DX mutant  : refused true · deactivate 12 · removeMembership 0
```

So a caller that ignores `refused` would suspend 12 people from a run the module has already judged untrustworthy. No fixture in the suite puts a non-empty `deactivate` under a membership-guard refusal — every RF-6 case has `deactivate: 0`, so `refused ? []` and `refusedDeactivations ? []` are indistinguishable there.

Fail-safe direction and reachable only through a caller that ignores `refused`, so a NIT. One fixture closes it — the shape above, asserting `plan.deactivate` is `[]` when only the membership guard fired.

## Carried from `f0d016be0`, still open

- **D2 (the DEACTIVATION ratio arm) still survives.** The new NIT-1 test covers the *membership* ratio band; the deactivation guard's three fixtures remain "large org loses most" (refuses), "small org over ratio" (floor allows it), "2 of 100" (below the floor too) — so nothing sits above `DEACTIVATION_FLOOR` and below `DEACTIVATION_RATIO`. Measured: `11 of 100` is allowed on the clean tree and refused under D2. The membership half now has the witness its twin still lacks; one fixture mirrors it.
- **R6 remains a source grep** (defeated by a dynamic `require` with no literal). Minor, unchanged reasoning: the module has no reason to reach a database and the grep catches the realistic regression.
- **Residual**: `completedEnumeration()` certifies provenance, not truthfulness — slice 2 must not unwrap a failed run and re-wrap its arrays. Re-confirmed on this SHA.
