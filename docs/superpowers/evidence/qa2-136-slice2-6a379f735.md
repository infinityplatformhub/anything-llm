# QA-2 — #136 slice 2 (offboardUser) — 6a379f735 — PASS (written by PMO from QA-2's body)
25 green / 5 red; all 5 red labelled [#135 lane] (P4 rollback ×3, P5 User.delete call sites ×2) — out of scope by contract.
P0 witness green; P1 successor inherits neither; P2 same FilterCache instance stale; P3 revocation per grant with correct role ids, survives deletion (7 assertions). Read: enumerates before removing, drives removeGroupMember / revokeGrant / revokeDocumentAcl per (role, workspace), no second implementation. All-or-nothing verified by failing the LAST step via $use: grants 2→2, acl 1→1, revocations 0.
Fix mutants: V1 skip ACL revocations — SURVIVED first (see finding), caught after oracle fix; V2 raw deleteMany → P3; V3 enumerate-after-remove → P3 ×2; V4 ignore workspace_id → P2.
FINDING (not this SHA's bug): document_acl effect="allow" rows are NOT a grant path for user actors — documentFilter.js:96 reads only effect="deny"; engine returns no_grants for an ACL-only actor. So an orphaned allow-ACL is INERT today; the recycled-id escalation runs through role grants only. ACL cleanup = defence-in-depth, lower severity than "recycled id inherits document access". Oracle now asserts the ROW is gone, not the engine answer (durable if allow-ACLs become load-bearing).
#139: 1e717b980 re-run = same FAIL (npx route), ledger-only delta.
