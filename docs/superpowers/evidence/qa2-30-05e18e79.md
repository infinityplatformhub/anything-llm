# QA-2 evidence — #30 slice 1a 05e18e79 — FAIL (LanceDB PASS 6/6; pgvector blocker)

Author: QA-2 (anything-llm-e6), transcribed verbatim by PMO. Containers qa2-30 / qa2-30c, dropped.

## LanceDB — PASS 6/6 + independent probes
- C1 S-10 cross-workspace/cross-org denied both flag states; same-org sibling ws denied.
- C2 S-17 foreign rows ranked above own (40 foreign on query vector, 2 own far): unfiltered topN=2 = foreign only; filtered topN=2 = both own; topN=1 = own; rerank path ACL-filtered; deny-list rows do not consume slots.
- C3 flag OFF: unlabelled cut at predicate + isRowAllowed; ON: passes both layers (not inert — the aa437ade fix).
- C4 half-labelled both states: `{orgId, ws:null}` denied; `{orgId, ws, docId:null}` allowed by design until a deny/allow list exists, then refused. Pushdown and isRowAllowed agree on every combination. `aclMetadataFor` throws without docId, so this shape cannot be created after T-5.
- C5 other-org denied both states. C6 matchNone → `toSqlString()` null, query skipped.
- Mutations: bare → `No field named orgid`; double-quote → 0 rows silently; regex confirms every identifier backticked incl. IS NULL clause.

## policy.changed + dead-bus — PASS 13/13 on real `policy_versions`
Version bump beats cache without any event; `isStale` detects; `invalidateScopes` scoped, `org:1` cascades; malformed event (`{}`, `{data:{}}`, `scopeKeys:[]`, string, null) → invalidateAll; dead bus → `cache.disable()` (rebuild every call, size 0, no stale) and re-enable on retry; register idempotent under 3 concurrent calls; allow-list + action in cache key.

## BLOCKER — pgvector `toJsonbSql` (`vectorPredicate.js:201-236`)
`next()` computes `$${startIndex + params.length}` after push for orgId but before push for arrays:
```
metadata->>'orgId' = $4 AND metadata->>'workspaceId' = ANY($4)
PARAMS: ["1", ["7"]]
```
`$3` never referenced, `$4` shared by two clauses. On real pgvector every shape throws: orgWide → `could not determine data type of parameter $3`; org+ws / +deny / +allow / all four → `op ANY/ALL (array) requires array on right side`. Fail-closed, no leak, but pgvector retrieval dead 100%. Present since c60d69d0; not on approof/main.
Verified fix on DB: `const bind = (v) => { params.push(v); return \`$${startIndex + params.length - 1}\`; };` → returns `["mine1"]` on deny+allow fixture.
Why untested: `legacyRowFlagHttp.test.js:176` only JSON.stringify-compares strict≠lenient; SQL never executed.

## pgvector passes
P7 JSONB key case-sensitive (`'orgid'` → 0 rows, no fold). P8 docId is a bound param (`x' OR '1'='1` not in SQL). P6 matchNone → null.

## Suite
127 suites / 1303 tests on clean container; 3 initial FAILs were harness (parallel suites on one prisma advisory lock + modelPricing flake), `--runInBand` 30/30.
