# QA-3 — #121 `e3fbcf775` (Dev1, `approof/121-frontend-role-sites`, tier `plain`)

**Verdict: FAIL (harness).** `yarn test` exits **1** on this SHA — 26 unhandled rejections,
all originating in the new `capabilityGating.test.jsx`. Behavioural findings below are
recorded against this SHA but must be re-fired on the fix.

Worktree `/tmp/qa3-104` @ `e3fbcf775`. Node 22, `frontend/`, vitest.

## 1. Exit code — checked first

```
yarn test  → EXIT=1
 Test Files  15 passed (15)
      Tests  105 passed (105)
⎯ Unhandled Errors ⎯  Vitest caught 26 unhandled errors during the test run.
TypeError: default.fetchSupportEmail is not a function
 ❯ fetchSupportEmail src/components/SettingsSidebar/index.jsx:206:41
TypeError: default.fetchCustomFooterIcons is not a function
```

Green output, exit 1 — the `b6ed23711` shape. All 26 are attributed to
`"src/components/SettingsSidebar/capabilityGating.test.jsx"`; the file's `vi.mock("@/models/system")`
supplies `keys`, `fetchLogo`, `isMultiUserMode` but not `fetchSupportEmail` or
`fetchCustomFooterIcons`, which `SettingsSidebar` calls from a `useEffect`. Rejections
land after the assertions, so every test still passes.

Deterministic, not flake:

| run | exit | unhandled |
|---|---|---|
| full suite ×3 | 1, 1, 1 | 26, 26, 26 |
| that file alone | 1 | 13 tests passed, rejections present |
| **parent `e3fbcf775^`** | **0** | 92 passed, none |

So it is introduced by this SHA, not inherited. `yarn check:capabilities` → **exit 0**,
`PASS capabilities cache: 9 checks`.

Fix is a mock completion, not a design change: add both functions to the `@/models/system`
mock (or await the effect). Cheap — but it must be a new SHA, because the suite as merged
would keep CI red.

## 2. First mutation — delete every line the diff added, one file at a time

| # | mutation | result |
|---|---|---|
| M1 | `MenuOption/index.jsx` reverted to `e3fbcf775^` | **killed** — 7 failed / 6 passed |
| M2 | `SettingsSidebar/index.jsx` reverted to `e3fbcf775^` | **killed** — 8 failed / 5 passed |

Neither file is decorative; the suite fails loudly if either half is absent.

## 3. Behavioural mutants

| # | mutation | result | killer |
|---|---|---|---|
| M3 | drop `if (hidden) return false` | **killed**, 1 failed | RF-5 `an admin holding system.write does not see it` |
| M4 | drop `if (flex && !user) return true` | **killed**, 3 failed | both RF-2 tests + RF-5 single-user |
| M5 | drop `if (loading && (capability \|\| capabilities)) return null` | **killed**, 1 failed | RF-6 |
| **M6** | `capabilities.some(...)` → `.every(...)` | **SURVIVES** — 13 passed | — |
| **M7** | `hasVisibleOptions` passes `hidden: false` for children (parent stops honouring a child's `hidden`) | **SURVIVES** — 13 passed | — |

PMO's requested checks all hold: a fixture holding one capability shows that site and
hides the others (RF-1, 13 tests); `user === null` sees every flex entry and the three
non-flex entries (users/workspaces/invites) stay hidden as on main (RF-2, and M4 kills it);
the parent disappears when no child has a capability and returns when one does (RF-3);
a multi-user admin holding `system.write` still does not see Security (RF-5, M3 kills it);
loading renders nothing until false (RF-6, M5 kills it).

### M6 and M7 are unreachable today, not gaps in the assertions

- **M6**: `grep -rn "capabilities:\s*\[\|capabilities={\[" SettingsSidebar/` → **nothing**.
  No caller in this file passes the plural `capabilities` array; the docblock names
  `ManageWorkspace` as the intended user, which is slice 2 / another component. The branch
  is dead code in this SHA, so no test can kill the mutant.
- **M7**: `hasVisibleOptions` shares `isEntryVisible`, which is the point of the refactor.
  Its `hidden` argument only matters when a *child* carries `hidden` and is the *only*
  otherwise-visible child of a parent. The children carrying `hidden` are
  `telegram`/`scheduled-jobs` (`hidden: !!user`) and `embeds`/`event-logs`
  (`hidden: !canViewChatHistory`), and the fixture pins `viewable: true` with a user set,
  so no test reaches that combination.

Neither is a defect **in this SHA** — both are latent. M6 becomes live the moment a caller
passes `capabilities: [...]` (slice 2, per the docblock), and M7 the moment a parent's last
visible child is a `hidden` one. Worth one line each in residual-risks so slice 2 arrives
knowing the `some` semantics are currently unproven.

## 4. Note on the commit

The ledger for #121 is not in the commit (`git show --stat` lists three files, all under
`frontend/src/components/SettingsSidebar/`). Flagging for the §7.7 record, not as a code
finding.

## Housekeeping

All mutations reverted; `git status --porcelain` clean in the worktree. No commits.
