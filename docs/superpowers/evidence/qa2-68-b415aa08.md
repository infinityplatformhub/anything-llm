# QA-2 evidence — #68 b415aa08 — PASS 4/4 (probe 32/32, mutations 6/6)
Author: QA-2 (anything-llm-e6), transcribed by PMO.
1 CHECK: 18 cases incl. UPDATE path, name does not pick the rule (saml-named LDAP shape passes), certificates:[] passes, rejection names constraint. 2 migrate on live data: valid SAML rows survive; half-configured row fails loudly at migrate (no NOT VALID). 3 is_nullable NO + pg_constraint.convalidated + raw NULL rejected at column. 4 limiter: 4th same-username 429 while other usernames pass; spray 21st 429; correct password still limited; 429 never reaches directory; empty username one bucket; case/whitespace normalized; X-Forwarded-For ignored (socket address only). Probe bug: XFF probe was invalid — driven middleware directly.
Suite 1846/1 failed (regression.test.js revokedAt 501 — cross-suite interference, solo 39/39).
