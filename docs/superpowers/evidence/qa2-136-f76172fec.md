# QA-2 — #136 fix — f76172fec — PASS (written by PMO from QA-2's body)
A 29/0 · B 5/0 · C 6/0 · D 36/0 (76 assertions). Every expected flip landed (R1, R2, D4, D6×2, F1, F1b×2, F1c×2, F4×4); must-not-move set held (D1 single-user null key, D2, D3, D5×2, controls, all A/C).
Fix mutants: V1 dangling→active RED (D7); V2 suspended→active RED (4); V4 catch{} fail-open RED (D8); V5 gate accepts non-suspended RED (D7); V3 stub-shape guard fail-open SURVIVES — unreachable from production (all 8 HTTP call sites pass no db; default prisma has findUnique); defence for test doubles; NIT, ledger note.
New probes: D7 raw-prisma user delete bypassing sweep (live unstamped key → dead id) — only assertion killing V1/V5; D8 $use throws on reader's users.findUnique with controls — only assertion killing V4.
Runner fix: migrate/seed exit codes were ignored → half-seeded DB produced uniform 501s (false FAIL once); runner now checks exits and counts roles/permissions before running.
