# QA-3 — #121 `5c9ea893d` (Dev1 + TL-1 F-B, tier **auth**)

**Verdict: PASS.** Every mutant in the plan dies, including M7 which survived on
`e3fbcf775`. The three visibility changes PMO asked me to assert are measured exactly and
match the oracle. Three findings, all **intended per TL-1**, recorded with their entry
lists.

Worktree `/tmp/qa3-127` @ `5c9ea893d`. Node 22. Own `yarn install` (frontend + server), own
database `qa3_121` (fresh `migrate deploy` + `seed.js`).

## 1. Exit codes — fired first

| | `e3fbcf775` | `5c9ea893d` |
|---|---|---|
| `yarn test` (frontend) | **exit 1**, 26 unhandled rejections | **exit 0**, 17 files / 124 tests |
| `yarn check:capabilities` | exit 0 | **exit 0** |
| `jest workspaceScopedCapabilities + assignableRolesHttp --runInBand` | (n/a) | **exit 0**, 41 passed |

The `fetchSupportEmail` / `fetchCustomFooterIcons` gap in the `@/models/system` mock is
closed; no unhandled rejections remain.

## 2. Exact visible sets, measured per real role

Holders re-measured on this SHA's own seed (not carried forward):

| action | org-scope holders |
|---|---|
| `system.write`, `system.read`, `user.read`, `invite.read` | `super_admin` |
| `settings.write`, `user.manage`, `key.manage` | `setup_admin`, `super_admin` |
| `chat.read_others` | `content_moderator`, `super_admin` |

Rendered the real `SettingsSidebar` with the capability map built from `ORG_CAPABILITIES`
(all 11) intersected with each role's real grants, menus expanded, multi-user:

| role | entries visible |
|---|---|
| `super_admin` | **26** — every entry except `security` (correctly `hidden` in multi-user) |
| `setup_admin` | **12** — mailer, workspaces, community-hub ×3, interface, branding, chat, embeds, api-keys, system-prompt-variables, browser-extension |
| `content_moderator` | **1** — workspace-chats |
| `member` | **0** |

### Finding 1 — `setup_admin` loses 14 visible entries (intended per TL-1; seed bug tracked as #137)

llm, vector-database, embedder, text-splitting, image-generation, voice-speech,
transcription, model-router, users, workspace-chats, invites, Default System Prompt,
event-logs, mobile-app.

(The oracle predicted 15 including `security`; `security` is `hidden` in multi-user for
every role, so 14 is the visible count. Oracle and measurement agree.)

Cause: `setup_admin` holds `settings.write`/`user.manage`/`key.manage` but not
`system.write`/`system.read`/`user.read`/`invite.read`. A role that exists to finish
installation cannot reach `llm`, `embedder` or `vector-database`. TL-1 ruled the seed is
the bug and the frontend is correct; recorded here as the exact list #137 has to fix.

### Finding 2 — legacy `manager` loses all 8 entries it had (intended per TL-1)

`ORG_ROLE_FOR_LEGACY` maps `manager → member`, and `member` holds none of the eight
actions. The eight sites previously `roles: ["admin","manager"]`: users, workspaces,
workspace-chats, invites, interface, branding, chat, browser-extension. Measured: `member`
sees **nothing**. The server already 403s these, so the UI now matches enforcement.

### Finding 3 — `content_moderator` gains workspace-chats (intended per TL-1)

The only widening in the change: `chat.read_others` is held by `content_moderator`, which
no role string previously admitted. Measured as its single visible entry.

## 3. Mutants — client

| # | mutation | result |
|---|---|---|
| M1 | `MenuOption/index.jsx` reverted to `9cf7b6fae` | **killed** — 7 failed |
| M2 | `SettingsSidebar/index.jsx` reverted to `9cf7b6fae` | **killed** — 9 failed |
| M3 | drop `if (hidden) return false` | **killed** — 3 failed (RF-3, RF-5, RF-7) |
| M4 | drop `if (flex && !user) return true` | **killed** — 3 failed (both RF-2, RF-5 single-user) |
| M5 | drop `if (loading && capability) return null` | **killed** — 1 failed (RF-6) |
| **M7** | `hasVisibleOptions` passes `hidden: false` for children | **killed** — 1 failed (`a parent whose only child is hidden disappears with it`) — **survived on `e3fbcf775`** |
| **F-B** | revert TL-1's fix: `hasChildren && !hasVisibleChildren` (drop `!visible \|\|`) | **killed** — 1 failed (RF-7 `a hidden parent renders nothing even when a child is visible`) |
| M8 | `if (!capability) return true` → `false` | **killed** — 11 failed |
| M9 | `return can(capability)` → `return true` | **killed** — 8 failed |

M6 (`capabilities.some` → `.every`) is **gone, not surviving**: the plural `capabilities`
prop was removed from `MenuOption` in this SHA — `grep capabilities` finds only a comment.
The dead branch I flagged on `e3fbcf775` was deleted rather than covered, which is the
better outcome. The `loading` guard is now `loading && capability` (singular), consistent.

## 4. Mutants — the server pairing (the fixture caveat, closed)

The client suite mocks `useCapabilities` directly, so it cannot see whether the endpoint
actually answers a key. `workspaceScopedCapabilities.test.js` holds that half. Fired
against the real database:

| # | mutation | result |
|---|---|---|
| S1 | remove all four new caps from `ORG_CAPABILITIES` | **killed** — 2 failed (both new tests) |
| S2 | remove `system.read` alone | **killed** — 2 failed |
| S3 | add an extra cap (`document.create`) — silent widening | **killed** — 1 failed (exact-list test) |
| S4 | sidebar gates on a capability absent from the list (`document.bulk_delete`) | **killed** — 1 failed (`every capability the sidebar gates on is answered by the map`) |

S4 is the one that matters: it is the direction the comment claims, and it holds. The
expectation is read from the sidebar source rather than restated, so it cannot drift into
agreeing with `ORG_CAPABILITIES`; the exact-list test catches deletions the sidebar never
mentioned. The two together close the gap I raised in the oracle — a frontend-only SHA
would have made 13 entries vanish for `super_admin` too, and S1/S2 now go red on exactly
that.

## 5. Housekeeping

All mutations reverted by file copy; probe test file deleted; `git status --porcelain`
clean in `/tmp/qa3-127`. No commits — evidence files are untracked in the main checkout for
PMO to commit. Database `qa3_121` left seeded and unmodified (no destructive probes ran
against it).
