# QA-2 — #122 `a69c2705b` — PASS (recorded by PMO from QA-2 message body; QA-2 is read-only)

Full `--runInBand` under 40 real bound backends (`SELECT 1` then `pg_sleep(600)`, active not idle) + `connection_limit=5`: suites 216/216 passed, tests 2983 passed / 36 pending, 0 failed; "too many clients" 0; PrismaClientInitializationError 0. Regime 2 (recon: 3×40 holders = every test red) is closed.

Open finding, not blocking: `connectionBudget.test.js:206` 500ms timeout — measured 4/6 at 500ms, 6/6 at 1000/2000ms; same case passes then fails on consecutive runs. New flaky in the suite that #122 exists to de-flake. Fix belongs in a follow-up.

Regime 1 not reproduced: the recon example suites (`viewAsUser`, `impersonationWrites`) were red only because of a missing `API_KEY_PEPPER` in QA's env.
