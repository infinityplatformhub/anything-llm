# QA-1 — #138 queue half `4c01ac1ca` — PASS (written by PMO from QA-1 body)

9 probes green; 6 mutants each with a DISTINCT red set:
```
BASELINE                 rf1 rf2 rf2b rf2c m3 rf3c rf4 rf5   all green
M1 lease map emptied     rf3c RED (only)
M2 claim unconditional   rf1  RED (only)
M3m lease never expires  rf2  RED + rf2c ERROR
M4 global lock in claim  rf4  ERROR + rf1 ERROR
M5 lease guard neutered  rf2c RED (only)
M6 key not from schedule rf5  RED (only)
```
RF-3c: `directory.sync:lark-…` → 160000 measured; negative control `telemetry.flush:x-…` → 30000. RF-6b: second click reuses job id, one pending row, key stable per minute.

**RF-2c** passes once the probe drives `applyDirectoryPlan` with `lease: {jobId, workerId}` (the real path). Earlier red was the probe bypassing the layer holding the guard. Live-lease control: same call under worker 2's valid lease → applied (6→7).

Harness bugs fixed before reporting: baseline classifier predated VERDICT field; nested `negativeControl.PASS` matched as verdict (manufactured survivor); mutation-present witness too narrow.

Residual: JobRuntime lease inheritance — `claimLeaseUsedByTick` 160000 makes telemetry.flush claimed at 160001 vs its own 30000 (delayed takeover, never concurrent); rf3d pins the value. `lark` not in provider registry → sync-now 404 for lark (#141).

Post-merge main 683f1987a: 3330 passed / 1 fail = doctor.test.js pgvector env (#146). `pgvector=0` printed in preconditions.
