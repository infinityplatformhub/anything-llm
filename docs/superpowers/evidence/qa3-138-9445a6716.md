# QA-3 — #138 permission slice `9445a6716` (Dev1, `approof/138-directory-sync-permission`)

**Verdict: PASS.** Every mutant dies, **G-h loudest** — the trap that shipped on
`fcf4236d4` is caught by a holder assertion, not only by a count. One merge-order flag in §5.

Worktree `/tmp/qa3-137` @ `9445a6716`. Own `yarn install` + `prisma generate`. Harness DBs
`qa3_138h` (suite) and `qa3_138g` (migration paths), both built fresh for this branch.
Harness: `/tmp/qa3-138h/{run.sh,mutate.py}` — refuses a dirty tree, a missing anchor, a
no-op edit, or a run with no `Tests:` summary.

## 1. Baseline

`npx jest directorySyncPermission --runInBand` → **exit 0, 10 passed**.
`npx jest __tests__/prisma --runInBand` → **exit 0, 5 suites / 33 passed**.

## 2. G-h first, as ordered

Omitting `directory.sync` from `ALL_ACTIONS` (seed vocabulary only; migration untouched):

```
RESULT G-h exit=1 -> Tests: 3 failed, 7 passed, 10 total
  ✕ HOLDER assertion: super_admin's seeded set contains it, setup_admin's does not
  ✕ directory.sync is in ALL_ACTIONS
  ✕ the migrated vocabulary IS the seed's ALL_ACTIONS, both directions
```

This is the result I asked for in the oracle. **The holder assertion is among the three** —
so the suite proves a role actually ends up holding the action on a seed-sourced install,
not merely that a literal was updated. That is precisely the gap TL-1 rejected `fcf4236d4`
for, and it is closed here by construction rather than by a count.

## 3. The rest

| # | mutation | result | killed by |
|---|---|---|---|
| G-a | migration creates a different action | **RED** (4) | migrations-alone, idempotency, vocabulary both-directions, permission-row-exists |
| G-d | also grant to `setup_admin` | **RED** (2) | migrations-alone; `super_admin ALLOWED / setup_admin DENIED` |
| G-e | also grant to `member` | **RED** (1) | an ordinary member is denied |
| G-f | delete the `policy_versions` INSERT | **RED** (2) | idempotency; the migration bumps the policy version |
| G-i | migration grants nothing (join on a missing action) | **RED** (2) | migrations-alone; idempotency |
| A-c | permission row created, granted to nobody | **RED** (2) | migrations-alone; idempotency |

G-i needed re-anchoring in my harness (the `WHERE p."action"` string I expected is a
`JOIN … ON p."action"` here); the harness **refused to run** rather than reporting a
survivor, which is the §7.17 behaviour.

## 4. Migration paths, measured on `qa3_138g`

**G-j upgrade path** — deploy with the migration withheld, then restore it:

```
deploy without 150000 -> exit 0, directory.sync rows: 0
deploy with 150000    -> Applying migration `20260902150000_directory_sync_permission`
                      -> directory.sync rows: 1
```

**G-g idempotency**, by row count rather than absence of an error:

| table | before | after replay |
|---|---|---|
| `role_permissions` | 122 | **122** |
| `permissions` | 63 | **63** |
| `policy_versions` | 12 | 13 (append-only, as in #137) |

**Seed-only path**: holders of `directory.sync` = **`super_admin:org`**, single row. ✅

## 5. Engine decisions, with an allow control

```
setup_admin directory.sync -> false (no_permission_in_roles)
setup_admin settings.write -> true  (allowed_by_role)      <- control
super_admin directory.sync -> true  (allowed_by_role)
super_admin settings.write -> true  (allowed_by_role)
```

My first run used `system.read` as the control and got `false` for it too — correct on this
branch, because **#137 is not in this branch's history** (`git merge-base --is-ancestor
edd8db6b7 HEAD` → not an ancestor), so `setup_admin` does not yet hold `system.read`. Swapped
to `settings.write`, which it holds on both branches. Recording it because a control that
happens to be false makes a deny probe indistinguishable from a broken one.

## 6. **The pin collision — flagged, as asked**

This branch measures **vocabulary 63**, and its own `vocabulary-diff` pin reads
`expect(ALL_ACTIONS.length).toBe(63)` — changed from 62 in this commit. `audit.purge` is
**absent** here (0 rows): #137 is not an ancestor.

So both #137 and #138 independently move the pin **62 → 63**, each correct in isolation and
each claiming the same number for a different action. Whichever merges second will conflict
on that line, and the resolution must be **64**, with both `audit.purge` and `directory.sync`
in `ALL_ACTIONS`. A resolution that keeps 63 passes its own file and fails the other suite's
holder assertion.

Dev1's ledger says the failure is how the merge order gets noticed; I confirm the mechanism
works, and add that the *number to resolve to* is 64 — worth stating explicitly so the
conflict is resolved by adding rather than by picking a side.

## 7. Housekeeping

All mutations restored by `git checkout -- .`, verified clean before and after each run.
`qa3_138h`/`qa3_138g` are throwaways; `qa3_121`, `qa3_137`, `qa3_138` untouched. No commits.
