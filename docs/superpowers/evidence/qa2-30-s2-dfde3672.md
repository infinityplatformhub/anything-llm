# QA-2 evidence — #30 slice 2 dfde3672 (superseded by round 3) — PASS on /v1 + embed + router lane; 1 pre-existing finding
Author: QA-2 (anything-llm-e6), transcribed by PMO. Probe 17/17, mutations 12/12, suite 158/158 (36 skipped real-store).
Finding (→ new issue): embed principal has no grants anywhere → matchNone for every embed; leak tests pass vacuously without a readable control. Pinned contract tests added; deny-wins verified via allowedDocumentIds.
Pass: denied pinned doc absent from LLM-visible prompt (chatSync + streamChat, with readable controls); documentId NULL cut by default, admitted under flag but deny still cut; getContextFiles throw / systemActor []; router denied doc absent, no user → nothing; cache staleness raw vs repository at served content; null actor → fail-closed everywhere.
