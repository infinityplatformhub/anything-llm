# QA-3 — #138 combined `27e60402e` both-shape check — PASS (written by PMO from QA-3 body)

Read-only probe, node 22, fresh DBs qa3_138cA/cB/dA/dB.

**Perm half unchanged by rebase:** `git diff 17e204f41 27e60402e --stat -- server/prisma` empty; `git diff a4f2a5753 17e204f41 -- server/prisma/seed.js` comments only (+15/−6). Re-measured anyway.

```
=== SHAPE A (migrate deploy + seed) ===      === SHAPE B (db push + seed) ===
 directory.sync | directory | org          directory.sync | directory | org
 org.member     | org       | org          org.member     | org       | org
```
`org.member` scope=org on shape B confirms the #53 seed-only gap is closed on the combined tree.

| | A | B |
|---|---|---|
| rows | 63 | 63 |
| sorted (action\|category\|scope) | identical, no diff | |
| category='' | 0 | 0 |
| scope null/'' | 0 | 0 |
| audit.purge | 0 | 0 |

**Pin:** vocabulary-diff.test.js:78 `ALL_ACTIONS.length` 63 — correct here (#137's audit.purge not in this history). Must resolve to **64** when #137 and #138 meet; a resolution keeping 63 passes its own file and fails the other side's holder assertion (qa3-138-9445a6716.md §6).

Suites: directorySyncPermission 16/16 exit 0; `__tests__/prisma` 5 suites / 33 exit 0. routeGateSweep not run (outside scope).
