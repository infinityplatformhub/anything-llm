# Techlead-2 — #119 `0e2317834`: **PASS** (QA-2 FAIL ปิดแล้ว)

worktree `/tmp/tl2-119b` (detached) donor `/tmp/qa2-84b` DB `t98b`
diff จาก `c44b059d3`: `sealRoutes.js` +215/-83 (แยก `sealTarget`), test +107, ledger +38

**baseline 44/44** (จาก 38 บน `c44b059d3` เมื่อนับเฉพาะไฟล์นี้)

---

## QA-2 สี่รูปทรงปิดหมด — ยิงเองบน app จริง `NODE_ENV=production`

```
=== QA-2 four shapes (must THROW) ===
app._router.post                       THROW
app._router.all                        THROW
app._router.route().post               THROW
app._router.use(Router)                THROW
=== legit (must MOUNT) ===
app.get                                MOUNTED
app.head                               MOUNTED
app.use('/', fn)                       MOUNTED
app.use(express.static)                MOUNTED
app.use(express.json())                MOUNTED
app._router.use(plain fn)              MOUNTED
app._router.route().get                MOUNTED
=== previously-closed (must THROW) ===
app.post / app.all / app.route().post / app.use(Router)   THROW ทั้งสี่
=== residual (documented, must MOUNT) ===
app.param                              MOUNTED
app._router.param                      MOUNTED
```

**ตรงกับ ruling ที่ผมให้ทุกข้อ** และไม่มี regression บนสิ่งที่ปิดไว้แล้ว

## รูปแบบที่ใช้จริงตรงกับที่ผมวัดไว้

`sealTarget(target, { keepRouteFactory })` — `_router` ได้ `true` (`.route()` ยังเรียกได้
แต่คืน Route ที่ seal เมธอด mutating), target อื่นได้ `false` (ปฏิเสธ `.route()` ทั้งหมด) ·
seal `_router` **จากใน `sealTarget`** ไม่ใช่จาก callsite — `index.js:229` ยังเป็น
`sealRoutes(app, apiRouter)` เหมือนเดิม · **ดีกว่าที่ผมเสนอ**: callsite ไม่ต้องรู้ว่า
express เก็บ object ที่สอง

## mutation 7 ตัว — 5 แดง, 2 เขียว (วิเคราะห์แยก)

| # | mutation | ผล |
|---|---|---|
| **N1** | ไม่ seal `_router` เลย | **3 failed** — `the four shapes QA-2 mounted are all refused`, `HTTP: a route forced onto _router does not answer with its own marker`, `a Route from the sealed factory takes reads and refuses writes` |
| **N2** | seal `_router` ด้วย `keepRouteFactory: false` (= one-liner ที่ผมห้ามไว้) | **6 failed** — legit mounts ทั้งหมด รวม `the legitimate post-boot mounts index.js makes all still work` |
| **N3** | factory ยังเรียกได้ แต่คืน Route ที่**ไม่** seal | **2 failed** — `four shapes`, `sealed factory takes reads and refuses writes` |
| **N5** | `keepRouteFactory: true` กับ **ทุก** target | **3 failed** — `.route() is refused`, `F9`, `F7` |
| **N7** | `guardDisabled` คืน false เสมอ | **3 failed** รวม `ROUTE_MOUNT_GUARD=off leaves a use()d sub-router mountable AND writable` |
| **N4** | ตัด `internalRouter !== target` | **44/44 เขียว** |
| **N6** | ตัด `if (!target) return;` | **44/44 เขียว** |

**N2 คือหลักฐานว่า ruling ของผมถูก** — one-liner ที่ PMO เอนไปทำให้ legit mounts แดง 6 ตัว
ตรงตามที่ผมวัดไว้ก่อน merge · **N5 คือหลักฐานว่าการแยกโหมดจำเป็น** ไม่ใช่แค่เปิด
`keepRouteFactory` ทั่วกระดาน — `apiRouter` ต้องปฏิเสธ `.route()` เต็มรูปแบบ

### N4 และ N6 เขียว — ตรวจแล้วว่าเป็น equivalent mutant ไม่ใช่ช่องโหว่

ผมพิสูจน์ด้วยการรัน ไม่ใช่การให้เหตุผล:

```
Router()._router:      undefined
app._router===app:     false
app._router._router:   undefined
sealRoutes(undefined):  OK (ผ่าน `if (!target)`)
```

- **N4** (`internalRouter !== target`): บน express 4.22.1 ไม่มีรูปทรงไหนที่ `_router === target`
  · เป็น defensive check ที่ยิงไม่ถึง · **ยอมรับได้** — ราคาคือหนึ่งการเปรียบเทียบ
  และมันกัน infinite recursion ถ้า express เปลี่ยน
- **N6** (`if (!target) return`): **ยิงถึงจริง** — `sealRoutes(undefined)` ไม่ throw ·
  แต่เทสไม่มีตัวไหนส่ง `undefined` เข้าไป · เทส `sealRoutes skips a target with no
  internal router` ทดสอบ `_router` ที่ยังไม่มี (คนละเรื่องกับ target ที่เป็น undefined) ·
  **NIT**: เพิ่ม `expect(() => sealRoutes(undefined)).not.toThrow()` หนึ่งบรรทัด
  ก็ปิด N6 ได้ · ไม่ block เพราะทิศทางปลอดภัยและไม่มี callsite ที่ส่ง undefined

## เทสใหม่ 6 ตัว — oracle ถูกทุกตัว

`HTTP: a route forced onto _router does not answer with its own marker` ใช้ **marker
ใน body** ไม่ใช่ status และเขียน comment ว่าทำไม (`app.all("*")` ทำให้ 200 ไม่มีความหมาย) —
ตรงกับที่ผมและ Dev5 สรุปไว้รอบก่อน · **นี่คือบทเรียนจาก F4 ที่ผมสั่งผิดเอง ถูกนำมาใช้ถูกที่**

`a Route from the sealed factory takes reads and refuses writes` แดงใน **3 mutation
ที่ต่างกัน** (N1/N2/N3) — assert ทั้งสองด้านของเส้นแบ่งจริง ไม่ใช่ด้านเดียว

`RESIDUAL: param() is not sealed` assert ทั้ง `app.param` และ `router.param` และเขียน
เหตุผล (ไม่ mount route ไม่เพิ่ม verb) · ถ้าปิดวันหลัง เทสแดงบังคับให้ตัดสินใจ —
ตามรูปแบบ #98 ที่ผมขอ

## comment `get`/`head` แก้แล้ว และแก้ถูก

JSDoc ใหม่แยกสองกรณีชัด: exemption จริงบน **app**, บน **router ธรรมดา** `get`/`head`
ถูกปฏิเสธเป็นผลพลอยได้ของการปฏิเสธ `.route()` — *"measured, not intended"* · และชี้ทางแก้
ถ้าวันหนึ่งต้องการ (`keepRouteFactory` ไม่ใช่เจาะรู) · ตรงกับที่ผมวัดได้ (`THROW api.get`)

## Verdict

**PASS** — ไม่มี blocker

- QA-2 สี่รูปทรงปิดหมด ยิงเองบน app จริง · legit เจ็ดตัวยัง MOUNTED
- mutation 7 ตัว: 5 แดงคนละชุด, 2 เขียวที่พิสูจน์แล้วว่าเป็น equivalent/ยิงไม่ถึงเทส
- **N2 ยืนยัน ruling ของผม**: one-liner ทำให้ legit แดง 6 ตัว · **N5 ยืนยันว่าต้องแยกโหมด**
- NIT เดียว: เพิ่ม `expect(() => sealRoutes(undefined)).not.toThrow()` เพื่อปิด N6
