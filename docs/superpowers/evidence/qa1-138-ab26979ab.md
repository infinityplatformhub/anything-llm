# QA-1 — #138 driver half — ab26979ab (source = cd2c6cabe) — PASS (written by PMO from QA-1's body)
Harness /tmp/qa138 (accept-then-silent server, fresh provider per case), tree /tmp/qa1-138b clean after every mutation.
| case | main | ab26979ab |
| A page silent | HUNG, page hits 1 | REJECTED 41.5s, page hits 4 (timeout per attempt, loop iterates) |
| B token silent | HUNG | REJECTED 10.0s, page hits 0 (token call is what was bounded) |
| D Retry-After 86400 | HUNG | RESOLVED 30.0s = clamp, both bounds |
| C caller 1s vs driver 30s | 2.5s | 1.00s (not 1s+backoffs → cancel not retried) |
| E caller never fires, driver 3s | HUNG | REJECTED 13.5s (driver wins) |
Mutants: 1 no timeout → A,B,D,E hang; 2 bind _page only → B alone; 3 drop clamp → D exceeds ceiling; 4 drop AbortSignal.any → C alone (1s→30s); 5 clamp 0 → D 10ms (undercuts floor). 2 ⊂ 1 with A/D/E green under 2 = token path has its own fixture; 3 and 5 are opposite failures on D — ceiling AND floor both required.
Lease note for queue half: silent-socket worst ≈ 41.5s (backoff ceiling 2000ms is not the dominant term); 429-on-every-page worst = (timeout + max(backoff, MAX_RETRY_AFTER_MS)) × (maxRetries+1) = 160s. Derive the lease from that expression.
