# QA-1 evidence — #30 T-5 slice 2 @ `4737f574`

**Verdict: PASS**

Worktree `/tmp/qa1-s2c` (detached, QA-owned). Probes `/tmp/ps2c/`. DB `qa1_s2c` (fresh,
`migrate deploy`, 22 migrations). Node 22. Per §7.14 no full suite — probe + mutation on
related files only.

Prior rounds: `78cdbecb` FAIL (cross-workspace BLOCKER + M7 survivor), `8e304b2c` FAIL
(telegram `actor` ReferenceError). Both closed here.

## 1. BLOCKER from round 2 is closed

`DocumentManager.pinnedDocs` now consults the filter's POSITIVE scope
(`utils/DocumentManager/index.js:80-88`), not only its exclusions.

| probe | round 2 (`78cdbecb`) | round 3 (`4737f574`) |
|---|---|---|
| carol (member of wsA only), `authorizedPinnedDocs({workspace: wsB})` | `["WORKSPACE-B-ONLY-SECRET"]` **LEAK** | `[]` |
| carol on wsA (control) | 2 docs | 2 docs, unchanged |

Over real HTTP, actor holding `chat.send` **org-wide** (`member:org`) so
`validWorkspaceSlug` — a loader, not a gate — lets the request through to wsB:

- `POST /api/workspace/s2-ws-b/stream-chat` → 200, bridge invoked with `{ws:2, n:0}`,
  wsB's pinned text absent from both prompt and body.
- `POST /api/v1/workspace/s2-ws-b/chat` → 403 at ingress, `auth.key_used` records
  `allowed=false denyReason=grant`. Never reaches the bridge.
- **Positive control, same route, same actor**: after granting carol `viewer:workspace`
  on wsB, the identical request returns `{ws:2, n:1, texts:["WORKSPACE-B-ONLY-SECRET"]}`.
  Without this the `[]` above would be equally consistent with a dead path.

## 2. Telegram path

`collectPinnedDocs(workspace, LLMConnector, actor)` — `actor` is a parameter
(`utils/telegramBot/chat/stream.js:204`), passed at the call site (`:125`). Driven by
executing the exported handler:

- alice → no throw, bridge called once, actor reached it (`actorId=1`), 2 docs
- bob (denied doc 2) → no throw, `actorId=2`, sees only `PINNED-OPEN-SECRET`; no sent
  message contains `PINNED-DENIED-SECRET`
- no actor → throws `streamResponse requires a resolved actor`

## 3. Baseline properties (all hold)

- alice 2 docs / bob 1 doc — deny enforced
- no user → `[]`
- `documentId NULL` excluded by default with an explicit log; admitted only under
  `RETRIEVAL_FILTER_ALLOW_UNPROVABLE`, where an explicit deny still wins
- `getContextFiles(ws, null, null)` throws; `{systemActor: true}` → `[]`
- all 10 call sites go through `authorizedPinnedDocs`; none omits identity.
  `.pinnedDocs(` / `.pinnedDocuments(` appear at exactly 2 places: DocumentManager itself
  and the bridge. No bypass.

## 4. Cache staleness — correct path now exists

- `repository.grantDocumentAcl` / `revokeDocumentAcl` present.
- `grantDocumentAcl` deny: `policy_versions` 19 → 20, deny effective **immediately**
  (alice loses `PINNED-OPEN-SECRET` on the next call, no TTL wait). Revoke restores it.
- Raw prisma write: 21 → 21, no bump, deny stale. Unchanged and acceptable — the
  supported path exists and bumps.

## 5. Embed / allow-list

An embed's `allowedDocumentIds` cannot escape its workspace scope:

- embed scoped to wsA, allow-list containing wsB's document, asked about wsB → `[]`
- control: same allow-list, scope wsB → `["WORKSPACE-B-ONLY-SECRET"]`

`utils/chats/embed.js` and `utils/router/index.js` now thread
`actorRef: {type:"embed", id: uuid, workspaceIds:[workspace_id]}`, closing the
router-prefetch case where an embed resolved as nobody and silently lost its own pinned
documents (fail-closed but wrong).

## 6. Mutation — 6/6 killed

Related suites: `pinnedContextAcl`, `telegramPinnedActor`, `pinnedCrossWorkspaceHttp`.
Baseline 23/23 pass.

| # | mutation | result |
|---|---|---|
| M1 | drop the workspace-scope gate (`if (false)`) | **3 failed** |
| M2 | invert `orgWide !== true` → `=== true` | **4 failed** |
| M3 | drop `allowedDocumentIds` narrowing | **2 failed** (M3, M3b) |
| M7 | `systemActor` no longer returns `[]` | **1 failed** |
| M8 | `document.read` → `document.search` in the bridge | **1 failed** (M8b) |
| M9 | drop the `orgId` tenancy check | **1 failed** |

**M7 (survivor in round 2) is dead.** Round 2's test asserted `[]` on a workspace with no
parsed-file rows, so `[]` came back either way. The new fixture seeds a row; I confirmed
independently that the mutant leaks — `getContextFiles(ws, null, {systemActor:true})`
returns `["ALICE-PARSED-SECRET"]` under it, because `where({userId: undefined})` does not
filter.

## NIT-1 (non-blocking) — M8 dies on source text, not behaviour

M8 is killed by `M8b`, which greps `pinnedContext.js` for `action: "document.read"`.
The behavioural test beside it (`M8`) builds its two filters by calling
`retrievalFilterFor` directly with an explicit `action`, so it never observes which action
the **bridge** chooses; it passes under the mutation.

The source assertion is a real guard and the comment explains why mocking cannot work
here (`retrievalFilterFor` is destructured at import). But it is pinned to a spelling: a
refactor to `action: READ_ACTION`, or moving the constant one file away, leaves the guard
green while enforcing nothing. Behavioural closure would be to call `authorizedPinnedDocs`
itself against the same split-action fixture — the bridge picks the action, and the
document must not come back.

