# QA-2 evidence — #60 S3 LDAP route 17aadd2f — PASS 12/12 (probe 56/56; mutations 22/23, M8 equivalent; suite 155/155 1669/1669)
Author: QA-2 (anything-llm-e6), transcribed by PMO. Real HTTP stack + filter-parsing mock directory.
1 empty/whitespace/non-string password refused before any bind (userBinds=0). 2 seven injection payloads → 401, parseFilter sees one filter; legit `( ) *` in mail still logs in. 3 unknown/wrong/ambiguous/injection byte-identical (ambiguous needs base dc=example,dc=com; controls added). 4 service bind 49 → 503, anonymous-resolving service bind → 503, unreachable 503, bind referral 401. 5/9 POST 200 not swallowed by wildcard; GET 404 not 500; oidc control intact. 6 429 on 6th incl. correct password; /enabled limited. 7 no password in console/event_logs/body/headers/users. 8 ldap:// → 503 with zero network; LDAP_ALLOW_INSECURE=1 opens; ldap://+StartTLS ok. 10 /enabled single boolean key, no config leak, no auth, disabled → false + login 404. 11 reset link hidden (code-read; no frontend test runner). 12 StartTLS fail → startTls×1, binds 0.
M8 (referral clause in _classify) equivalent — fallthrough returns same error; M8b service-bind referral caught. Extra probes: internal error after bind not leaked; missing/array mail → 401 no user; subject = DN.
