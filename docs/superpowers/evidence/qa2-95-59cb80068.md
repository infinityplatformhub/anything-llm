# QA-2 — #95 59cb80068 — PASS

Probe 23/23 via `AuditEventSubscriber.handle()` → read back `event_logs.metadata` from Postgres. Dev suite `auditRedaction.test.js` 148/148.

| mutant | probe red | dev suite red |
|---|---|---|
| M1 all 3 patterns back to `\b` | 9 | 3 |
| M2 remove all lookarounds | 6 | 2 |
| M3 remove trailing `(?!\d)` on thai_national_id | 6 | 2 |
| M4 remove leading `(?<!\d)` on phone_th | 0 | 0 — **equivalent mutant**: PATTERNS order (thai → card → email → phone) means credit_card already claimed any digit run reaching phone_th; `"123450812345678"` → `[redacted:credit_card]` on both versions. No behavioural input separates them. |

Shapes redacted: `note_<13>`, `id<13>`, `x_<10>`, `mobile<10>`, `id_<16>`, `card<16>`, nested 3 deep, inside allowlisted free-text. Bound kept: 16 digits = one credit_card, 14 digits not 13+1. Negative controls: `ordinary_workspace_name`, 12 digits, `workspace_42_notes`, 13-digit **number** field untouched.

Fullwidth `１２３…`: never redacted before or after (`\d` ASCII-only) → separate issue.

Pre-existing (not #95 regression), separate issues:
- false positive: 13-digit ms timestamp stored as **string** → `[redacted:thai_national_id]` (e.g. `workspaceId: "1788342383220"`)
- evasion: ≥17 contiguous digits match no class (`99<13>99`)

Env note: suite needs `API_KEY_PEPPER` ≥32 bytes + `SIG_KEY`/`JWT_SECRET` or 2 tests fail at import.
