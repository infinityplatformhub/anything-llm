# QA-2 — #138 combined `27e60402e` — queue half PASS; route unreachable until #141 (written by PMO from QA-2 body)

**As shipped:** 9/6. super_admin gets 404 "No such directory-syncing identity provider." Not a permission problem (403 → 404 proves `directory.sync` seeded and held). Cause: directorySync.js:70 `!isKnownProvider(provider) || !providerCapabilities(provider).directorySync`; registry on this SHA: oidc/saml/ldap known with directorySync=false, lark NOT REGISTERED. No provider satisfies it; the route answers 404 to everyone. Code comment defers to S4a follow-up = #141.

**With lark temporarily registered** (2 lines, reverted): 15/15.
- S1 202, one job `directory.sync:lark` pending, no worker/lease
- S2 second click 202, same row, key has no per-request timestamp
- S3 setup_admin 403, nothing enqueued, still holds user.manage (allow control)
- S4 unauth 401, member 403, no refusal enqueued anything

**Mutants on real route/gate, all caught:** M1 gate→user.manage → S3 red; M2 direct jobs.create → S2 "second click ACCEPTED, not a 500" red (unique constraint keeps count 1, caller gets 500); M3 per-request key → S2 ×2 red.

Harness bugs found by disbelieving green: M2 first "passed" with 4 SKIPs (mutant broke parsing → module failed to load → 404 → SKIP); heredoc mangled the registration script. A mutant that breaks parsing tests nothing.

**Recommendation (scope call for PMO/TL):** queue half sound; shipping alone leaves "sync now" answering 404 for every provider until #141.
