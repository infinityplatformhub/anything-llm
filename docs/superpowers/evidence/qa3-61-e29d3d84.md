# QA-3 evidence — #61 V9 e29d3d84 — FAIL (8/9 pass; 5 findings)
Author: QA-3 (anything-llm-ea), transcribed by PMO. Own worktree, PG17, 3 DBs.
Pass: self-only (owner verified from DB), outsider 404 / viewer 200 self-only, read_others no widening, q length 400s + `%_\` literal + bad cursor 400, Thai substring prompt+response, `?schema=alt` migrate → pg_trgm in public, api_session/include hidden; Dev5 suite 21/21.
F1 (confirms Techlead): update-chat (2 routes) leaves response_text stale — old word still found, new not. F2: `upsert()` never sets response_text → agent chats unsearchable, overwrite stale. F3: `bulkCreate()` never sets it. F4: no EXPLAIN test (ruling Q5). F5: cluster `lc_ctype=C` → `show_trgm('ประวัติศาสตร์')='{}'` → Thai needle Seq Scan 10,005 rows; same DB with `LOCALE en_US.UTF-8` → 13 trigrams, Bitmap Index Scan. Migration rationale ("trgm because Thai") holds only under UTF-8 ctype; nothing checks or warns.
Rulings: F1–F4 in #61 (single derivation helper for every write path, EXPLAIN test); F5 detect in #61 (migration RAISE WARNING + boot error), enforce locale in O2.
