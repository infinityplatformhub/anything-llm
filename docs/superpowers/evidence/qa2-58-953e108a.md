# QA-2 evidence — #58 953e108a (ruling C revised) — PASS 12/12

Author: QA-2 (anything-llm-e6), transcribed verbatim by PMO (QA is read-only in repo).
Env: fresh DB, own container port 5487. `Test Suites: 124 passed, 124 total` / `Tests: 1250 passed, 1250 total`.

## 1. boot repair
- shape (b) → `multi_user_mode` becomes `true`; error-level log `[DEPLOYMENT SHAPE REPAIRED]`.
- 3 consecutive boots → log count [1,1,1] (every boot, not once).
- `MODE_REPAIR_ACKNOWLEDGED=1` → log=0 but repaired=true, setting=true (ack silences log only, never the repair).
- Consistent instance → untouched, no log.

## 2. runtime guard `POST /request-token`
- shape (b) + CORRECT AUTH_TOKEN → `401 [003] Invalid password provided`, `token: null`; body byte-identical to wrong-password response (no branch leak).
- Positive control: true single-user (0 users, `isConfirmedSingleUser=true`) → 200 mints token; wrong password on same instance → 401.

## 3. DB unreadable
- `users.count()` throws → `{repaired:false, reason:"unreadable"}`; setting not written (false before/after); log `could not read deployment shape`; no throw → boot continues.

## 4. Sweep items closed
- Extension key of suspended user in shape (b) → 403, `next` not called (previously passed).
- `validApiKey` → `locals.multiUserMode = true` in shape (b); 11 admin API routes no longer 401.
- Mobile `validRegistrationToken`: deleted user → `400 User not found`; suspended → `400 User is suspended`; normal admin → next called.
- `websocket.js:17` still raw per ruling D (confirmed).

## Raw `isMultiUserMode` reader count: 19 (not 14); remainder are not auth decisions
`system.js:236` (covered by guard 2), `system.js:784` (reports setting, correct after repair), `utils.js:22`, `systemSettings.js:470`, `PushNotifications:112`, gmail/calendar/outlook ×6 (feature-off in multi-user, safe direction), `defaults.js:127`, `websocket.js:17` (D), `markOnboarded.js:48`, `telegramBot:172`, `actorResolver.js:303` (must be raw: definition of `isConfirmedSingleUser`), `assertDeploymentShape.js:30` (must be raw: inspects the broken setting), `simpleSSOEnabled.js:22,66` (reads locals first, fallback raw).

## Non-blocking note for Dev2
`simpleSSOEnabled.js` fallback is raw setting; only matters if a route ever mounts it without `validatedRequest` first. All routes do today. Single future drift point. [→ residual-risks]

## Method note
Q58-4c first passed for the wrong reason (token sent in body; middleware reads `Authorization` header → `400 Registration token is required` before mode check). Fixed to header, re-ran 3 cases (deleted/suspended/normal).
