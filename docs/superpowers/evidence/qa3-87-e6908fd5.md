# QA-3 evidence — #87 `e6908fd5` — PASS

Author: QA-3 (anything-llm-ea). Worktree `/tmp/qa3-87`, own `yarn install` +
`prisma generate`, own database `qa3_87`. Probes written independently of Dev5's
tests.

This closes the disagreement QA-3 reported on #74: the doctor accepted
`VECTOR_DB=PGVECTOR` while the app fell through to LanceDB. The app now
normalises, so the preflight's spelling gate is removed rather than kept in
disagreement.

## Every provider, every spelling

Ten providers × five spellings each (exact, upper, capitalised, space-padded,
tab/newline-padded) = **50 resolutions, all correct**:

```
P1 50 spellings, all correct. sample: "pinecone" -> PineconeDB | "PINECONE" -> PineconeDB | "Pinecone" -> PineconeDB
```

`SUPPORTED_VECTOR_DBS` matches the ten arms `getVectorDbClass` actually
dispatches on — asserted as a set relation, not a hardcoded count, so adding a
provider without listing it fails here.

Normaliser edge cases:

| input | result |
|---|---|
| `undefined`, `null`, `""`, `"   "`, `"\t\n"` | `lancedb` |
| `"PGVECTOR"`, `"  PgVector  "` | `pgvector` |
| `"LANCEDB"` | `lancedb` |
| `"not-a-provider"` | `"not-a-provider"` — returned unchanged, so the caller can quote it |

That last row is what lets the error message name the operator's actual typo
rather than a normalised version of it.

## The fallback says what went wrong, once

```
[ENV ERROR] VECTOR_DB is "postgres-vector", which is not one of pinecone, chroma,
chromacloud, lancedb, weaviate, qdrant, milvus, zilliz, astra, pgvector. Using
lancedb instead — embeddings will be stored there, not where you configured.
```

| id | scenario | result |
|---|---|---|
| P3 | five calls with one unrecognised value | **1** log line; names the value; does not say "No VECTOR_DB value found" |
| P4 | two distinct typos, four calls | **2** lines — a second problem is still reported |
| P5 | `pgvecter`, `PGVECTER`, `  PgVecter  ` | **1** line — case variants of one typo are one problem |

P5 is the property the `Set` keyed on the *normalised* value gives: keying on the
raw string would have printed three lines for one mistake.

## The four comparisons now agree

| id | check | result |
|---|---|---|
| P6 | `supportedVectorDB` vs the resolver, over 54 inputs | **0 disagreements** |
| P7 | `doctor.requiredExtensions` vs which provider the app actually builds, over 12 spellings | agree on all |

P7 is the #74 residual, closed: `VECTOR_DB=PGVECTOR` now makes the doctor demand
`vector` **and** makes the app use pgvector — previously the doctor demanded it
while the app used LanceDB. `doctor.CHECK_IDS` no longer contains
`config.vector_db`, which is correct now that the app honours the spelling the
check used to block.

The removal is safe in the direction that matters: on stock `postgres:16` with
`VECTOR_DB=PGVECTOR`, the doctor now fails on `ext.available`/`ext.permitted`
(the extension really is needed and really is absent) rather than on a spelling
rule — a true refusal for the true reason.

## Mutation

| mutant | result |
|---|---|
| `resetAllVectorStores` reverted to a raw compare | **1 failed** |
| normaliser drops `.trim()` | **13 failed** |
| normaliser drops `.toLowerCase()` | **16 failed** |
| warn on every call (drop the `Set` guard) | **1 failed** |
| `supportedVectorDB` back to its own raw list | **1 failed** |

The first is the one worth naming: a misspelled key used to reach
`resetAllVectorStores` and take the **per-namespace** reset path while
`getVectorDbClass` handed it the pgvector provider — leaving the embedding table
in place with a vector dimension that can no longer change. Same provider, wrong
reset strategy, and nothing said so.

## Suites

`vectorDbSelection` + `__tests__/scripts`: **118/118**.
Per §7.14 the full suite is the gate's single run.
