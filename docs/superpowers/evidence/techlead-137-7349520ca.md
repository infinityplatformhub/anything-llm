# Techlead-1 — #137 `7349520ca` (auth): **PASS**

**Skills invoked:** `superpowers:requesting-code-review`; `security-review` checklist —
privilege escalation, audit integrity. `infi-lessons` not invoked here.

§7.14: no suite run. Two-hunk re-read as stated, plus `node -e` probes in a detached worktree
(`/tmp/tl-137d`, Node 22).

## F-1 closed, and the blast radius is what it should be

```
ALL_ACTIONS len: 63   has audit.purge: true      (was 62 / false)
setup_admin has audit.purge: false   | len 11    ← unchanged, confirmed
super_admin === ALL_ACTIONS: true                ← so it gains it, as intended
content_moderator has audit.purge: false
member has audit.purge: false
```

Only `super_admin` gains the action, and it gains it structurally (its list *is* `ALL_ACTIONS`)
rather than by a second edit that could drift from the migration. Every other role's literal is
untouched. That is the correct shape: the vocabulary is one place, the grant lists are another.

Re-ran the derivation that produced the rejection:

```
t1:186 equal now?  true
db-only: []   seed-only: []
```

The assertion that would have failed on merge now holds in both directions.

## F-2 closed, and RF-F2 is stronger than what I asked for

I asked for set equality on the migrations-only database. The test does that and adds three things
I did not specify, each of which closes a real hole:

- **Non-vacuity** (`seeded.size > 50`). Two empty sets are equal; without this the whole assertion is satisfiable by a failed query.
- **Both directions named separately** rather than one `toEqual` on sorted arrays — a failure says *which* direction broke instead of printing two 60-element arrays.
- **RF-F2b** pins the pair that matters: `audit.purge` **in** the vocabulary and **not** in `setup_admin`'s literal. That is the assertion that fails if someone "fixes" a future vocabulary mismatch by adding the action to the role instead.

The comment records the defect that shipped — 62 seeded vs 63 migrated, super_admin missing the new
action — rather than describing the test abstractly. A reader who breaks it learns what it caught.

One thing correctly flagged rather than papered over: `RETIRED_BY_LATER_MIGRATIONS` is duplicated
from `t1-authz-migration.test.js:184` because it is exported from nowhere, and the comment says so
and says this test failing is how you find out. That is the honest disposition; exporting it is a
tidy-up for whoever next retires an action, not for this SHA.

## The ledger wording is corrected, and correctly

> *"Deploy of an earlier-named migration onto an already-upgraded instance was measured and is
> FINE… The rename is hygiene; there was no deploy failure to fix."*

That matches what I measured on the pinned Prisma 5.3.1, and it keeps the next person from
renaming a migration to avoid a problem that does not exist. The retained reason — two directories
sharing a timestamp prefix order by directory name rather than by intent — is the real one.

## Verdict

**PASS.** Both findings closed with no new surface: one seed constant, one test block, one ledger
paragraph. Everything I passed on `fcf4236d4` is unchanged and still stands.

---

## Addendum — `edd8db6b7`: the second pinned count. **Confirm.**

**Skills:** `superpowers:requesting-code-review`.

`__tests__/prisma/vocabulary-diff.test.js:81` pin **updated to 63, not removed** — verified in the
diff, and `ALL_ACTIONS.length` measured at 63. `audit.purge` is added to the approved list with
its reason ("Gates DELETE /system/event-logs; super_admin only"), so the entry says what the action
is for rather than only that it is allowed. The pin keeps its comment explaining why it is pinned.
That is the right disposition: a literal count is a deliberate-edit gate, and updating it is the
work, not an obstacle to it.

Swept the rest of `ALL_ACTIONS`'s readers for the same dependency, since two is the number that
suggests a third:

```
workspaceCapabilities.test.js:138   expect(ALL_ACTIONS).toContain(action)     membership
myCapabilities.test.js:171          Object.keys(caps).length < seeded.length  inequality
routeScopes.test.js:94,127          every(... ALL_ACTIONS.includes(action))   membership
ssoIssueRetirement.test.js:84       not.toContain("sso.issue")                membership
t1-authz-migration.test.js:186      set equality                              (fixed)
```

**No third count pin.** Every other reader asserts membership or an inequality, both of which are
insensitive to the size of the vocabulary. So `vocabulary-diff` and the F-1 set-equality are the
complete set of things a new action breaks — the two that are now green.

Dev3's ledger note is the more useful half of this commit: the reviewer named one dependent and
running that one was not enough; `grep -rln ALL_ACTIONS __tests__` is the step, and a directory is
the unit. Worth keeping in §7.17 as written.
