# Techlead-2 — #98 `12f8732b8` (= `db9649c24`, ledger only): **PASS**

worktree `/tmp/tl2-98b` (detached `12f8732b8`) + `/tmp/tl2-98` (`71acf4173`, รอบก่อน) DB `t98b`
ยืนยันว่า `db9649c24` ไม่มี code delta: `git diff --name-only 12f8732b8 db9649c24` คืน
`.infi/ledger-98.md` ไฟล์เดียว — verdict บน `12f8732b8` ใช้กับ `db9649c24` ได้

**baseline: 2/2 suites, 60/60 tests, 0 failed** (จาก 52/52 รอบก่อน) วัดซ้ำหลัง mutation ครบ ยัง 60/60

---

## (ก) bypass ทั้งสามที่ผมยิงได้ในรอบก่อน — ปิดหมดแล้ว

ยิงสคริปต์เดิม (เดิน `app._router.stack` หา router layer จริง ไม่ได้จับ reference ก่อน seal)
บนทั้งสอง SHA:

| ทาง | `71acf4173` | `12f8732b8` |
|---|---|---|
| `app.post` / `apiRouter.post` | GUARD THROW | **GUARD THROW** |
| `app.all` | **MOUNTED 200** | **GUARD THROW** |
| `apiRouter.all` | **MOUNTED 200** (`{"pwned":"apiRouter.all"}`) | **GUARD THROW** |
| `.route(path).post()` | **MOUNTED 200** (`{"pwned":"route().post"}`) | **GUARD THROW** |
| `use(subRouter)` | MOUNTED 200 | **MOUNTED** (ประกาศไว้ → #119) |

**Dev3 เบี่ยงจาก ruling ของผมในทางที่ดีกว่า และผมยืนยันว่าถูก** — ผมเสนอให้ "บันทึก `app.all`
เป็น residual" เพราะคิดว่า `terminalNotFound` บังคับให้ `all` ต้องไม่ถูก seal · Dev3 เห็นว่า
ข้อจำกัดจริงคือ **404 ต้องเป็น layer สุดท้าย** ไม่ใช่ "ต้องอยู่หลัง seal" — ย้าย
`app.all("*")` ขึ้นไป**ก่อน** `sealRoutes()` แล้ว seal `all` ได้ทั้งตัว · ข้อจำกัดสองข้อ
(อยู่ท้าย stack / อยู่ในเขต seal) เข้ากันได้ ผมมองไม่ออกเอง

**`.route()` factory seal เป็นสิ่งที่ผมไม่ได้เสนอด้วยซ้ำ** — ผมรายงานว่ามันรอด Dev3 ปิดโดย
seal ตัว factory ไม่ใช่ไล่ seal Route ที่แจกออกไป ซึ่งเป็นทางที่ถูก: ไม่มี Route ไหนถูกแจก
หลัง boot อีกเลย

## (ข) 404 ยังทำงานถูก — ตรวจแล้ว ไม่ใช่ regression

`app.all("*")` ย้ายที่ จึงต้องพิสูจน์ว่ายังเป็น layer สุดท้าย: วัดจาก `app._router.stack`
แบบ recursive → **`last layer path: *`, total 318** ✓ และ `routeGateSweep` ยัง assert
`terminalWildcard === mountedRoutesAtTestLoad.at(-1)` เขียว

**พฤติกรรม HTTP ของ path ที่ไม่มีอยู่คืนอะไร** — ผมวัดทั้งสอง SHA:

```
                          71acf4173          12f8732b8
/api/nonexistent-xyz      POST 200 GET 200   POST 200 GET 200
/nonexistent-xyz          POST 200 GET 200   POST 200 GET 200
```

**เหมือนกันเป๊ะ** — 200 มาจาก SPA fallback (`app.use("/", IndexPage.generate)` ที่ `:175`
ในโหมด non-development) ซึ่งอยู่**ก่อน** wildcard เสมอทั้งสอง SHA · ไม่ใช่ผลของการย้าย
`app.all` และไม่ใช่ regression ของ #98 · บันทึกไว้เพราะการย้าย 404 เป็นการเปลี่ยนที่คนอ่าน
diff จะสงสัยเรื่องนี้เป็นอย่างแรก

## (ค) mutation

| # | mutation | ผล |
|---|---|---|
| **M1** | ถอด `"all"` ออกจาก `SEALED_METHODS` | **1 failed** — `A1: app.all and apiRouter.all are refused — 'all' does not route through 'post'` |
| **M2** | ถอด `.route()` factory seal | **1 failed** — `.route() is refused — it hands out a fresh Route the seal never wrapped` |
| **M3 (N7)** | ย้าย `app.all("*")` กลับไปใต้ `sealRoutes()` | **11 failed + 1 runtime error** — รวม `the terminal 404 still exists and is still last, despite 'all' being sealed`, `development boot still mounts /v/:command`, `production boots, and READ routes stay mountable` |
| **M4** | escape hatch presence-based (`!!env.ROUTE_MOUNT_GUARD`) | **8 failed** — `"false"`, `"0"`, `"no"`, `"OFF"`, `"Off"` … ทั้งตาราง typo |

**M3 คือมิวแทนที่สำคัญที่สุด** เพราะมันคือ "ทำตาม ruling เดิมของผม" — ถ้า Dev3 ทำตามที่ผมบอก
โดยไม่คิดต่อ ผลคือ 11 เทสแดง ไม่ใช่แค่ residual ที่บันทึกไว้ · การที่มันแดงหนักขนาดนี้ยืนยันว่า
ลำดับใหม่ไม่ใช่การจัดระเบียบ แต่เป็นเงื่อนไขความถูกต้อง

**M4 แดง 8 ตัวจากตาราง typo 9 เคส** — เป็นการยืนยันว่า exact-value ไม่ใช่คำพูดในคอมเมนต์
แต่มีเทสถือทีละค่า · asymmetry ที่ JSDoc อธิบาย (presence = เปิด gate → typo ปลอดภัย;
exact = ปิด guard → typo ปลอดภัย) ถูกต้องและตรงข้ามกับ `EMBED_REQUIRE_*` อย่างมีเหตุผล

## (ง) A2/A3 ที่ผมขอ — ผ่าน

- **A2** (fixture ต้องยิงผ่าน object ที่ endpoint จริงถือ ไม่ใช่ `apiRouter` ที่ import ไม่ได้):
  เทสเดิน `_router.stack` หา router layer เหมือนที่ผมทำ · ผมยืนยันเองอีกครั้งว่า object
  ที่ได้คือตัวจริง (`typeof .post === "function"`, regexp `/^\/api\/?(?=\/|$)/i`) และมัน THROW
- **A3** (ยิงซ้ำหลาย ๆ ครั้ง): เทสยิง 3 ครั้ง — seal ไม่ถูก restore หลัง throw ครั้งแรก
- **fixture `app.get` หลัง require**: เทส `production boots, and READ routes stay mountable
  on the real app afterwards` — ชื่อบอกตรง และ M3 ทำให้มันแดง ซึ่งพิสูจน์ว่ามันเรียก `app.get`
  หลัง require จริง ไม่ใช่เช็ค route ที่ mount ไปแล้ว (ข้อกังวลที่ผมยกไว้)

## (จ) `use(subRouter)` — ประกาศไว้ถูกและมีเทสถือ

ผมยิงยืนยัน: `apiRouter.use("/bypass-sub", sub)` ที่ `sub` มี `POST /deep` → **MOUNTED**
และ `POST /api/bypass-sub/deep` → **200** ยังเป็นรูจริง

**แต่การประกาศตอนนี้ถูกต้อง** ต่างจากรอบก่อน: JSDoc มีตาราง CAUGHT/NOT ที่ระบุ
`use(subRouter)` เป็น NOT พร้อมเหตุผลที่ถูก (*"`use` is how all middleware mounts, so sealing
it refuses the static handler, body parsers, and every legitimate late `use`"*) และมี
เทส assert ว่ามัน **ยัง mount ได้** — ซึ่งหมายความว่าถ้า #119 ปิดมัน เทสนั้นจะแดงและบังคับ
ให้ตัดสินใจ ไม่ใช่ปล่อยให้ claim เก่าค้าง · นี่คือรูปแบบที่ผมขอในเงื่อนไขข้อ 2 รอบก่อน

---

## Verdict

**PASS** — ไม่มี blocker · เงื่อนไขทั้ง 3 ข้อที่ผมตั้งไว้รอบก่อนทำครบ และสองข้อแรกถูกทำ
**ดีกว่าที่ขอ**: ผมขอให้ "บันทึก residual 4 ทาง" Dev3 กลับไป**ปิด 3 ใน 4** แล้วบันทึกเฉพาะ
ตัวที่ปิดไม่ได้จริง

- bypass ที่ผมวัดได้รอบก่อน (`app.all`, `apiRouter.all`, `.route().post()`) **ปิดหมด** —
  ยิงซ้ำด้วยสคริปต์เดิม GUARD THROW ทั้งสาม
- mutation 4 ตัว จับได้ทุกตัว แดงคนละชุด · M3 (ลำดับ 404) แดง 11 ซึ่งเป็นสัดส่วนที่ถูก
- 404 ยังเป็น layer สุดท้าย (318 layer, `last layer path: *`) และพฤติกรรม HTTP เหมือน
  pre-amend เป๊ะ — ไม่ใช่ regression
- residual ที่เหลือ (`use(subRouter)`, sub-router ที่จับ reference ก่อน seal) ประกาศตรงกับ
  ที่วัดได้ และมีเทสถือไว้ทั้งคู่

## Residual (บันทึก ไม่ใช่ข้อเรียกร้อง)

- **`use(subRouter)` → #119** — เป็นรูจริงและ HTTP ถึงได้ (ยืนยัน 200) แต่ปิดไม่ได้โดยไม่
  เปลี่ยนความหมายของ `use` · ข้อสังเกต: ทางนี้ **ยากกว่า** อีกสามทางในเชิงการเผลอทำ —
  ต้องสร้าง Router ใหม่แล้ว mount เข้าไป ไม่ใช่แค่พิมพ์ `.all` แทน `.post`
- **`ROUTE_MOUNT_GUARD=off` ปิด guard ทั้งหมด** รวมทั้ง `.route()` factory · log ทุก boot
  ตามที่ออกแบบ และมีเทสยืนยันว่า late mount ผ่านได้จริงเมื่อตั้งค่า — honest
