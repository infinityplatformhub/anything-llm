# QA-2 — #119 `0e2317834` — PASS (recorded by PMO from QA-2 message body; QA-2 is read-only)

Probe 30/30 + dev suite routeMountGuard 63/63 = 93/93.

Bypass closed on the shipped app: `app._router.post/all/route().post/use(Router)` all THROW; `stack.length` unchanged after the throw (no orphan layer); POST at the previously-hitting splice position returns no marker.
Legit paths mount: `app.get/head` (Route.methods.get true), `app.use(plain fn)`, `_router.use(plain fn)/static`, `_router.route("/x").get`; `_router.route("/x").post` THROWS. `keepRouteFactory` behaves as documented. `param` residual pinned as mountable.

Mutations 6/6 caught (probe/dev): M1 seal-no-throw 14/9; M2 throw-no-seal 10/6; M3 `.stack` 5/2; M4 GUARD=off-still-seals 2/2; M5 plain-seal `_router` (refuse `.route`) 3/6 — legit mounts red incl. "production boots"; M6 revert `_router` fix 3/3.

QA-2 self-corrections: (1) "late app.get must serve" is wrong — `app.all("*")` at index.js:227 is mounted before seal at :229, so anything mounted later sits behind the catch-all regardless; assert stack + `route.methods.get` instead. (2) same for late middleware: terminal ends the chain; on a self-built app with catch-all last, assert it runs. (3) M1/M2 first reported 0 red because the anchor's indent changed and the mutation never applied; require `count(anchor)==1` before trusting a result.
