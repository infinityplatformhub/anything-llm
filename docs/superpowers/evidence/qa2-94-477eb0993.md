# QA-2 — #94 477eb0993 — PASS

Probe 58/58 (38 prior + 18 section H + 2 observation), dev `bundle.test.js` 41/41. CLI verified: `DATABASE_URL=postgresql://qa2_leak_probe:Sup3rSecret@localhost:55451/qa node scripts/doctor.js --bundle` → username False, password False, `redactions: ['credit_card','url_credentials']`, `db.reachable` detail `Connected to postgresql://localhost:55451/qa.`; stderr keeps masked full form for the operator.

describe.each 3 hosts (`postgres`, `localhost`, `db.internal.corp`) × safeQuery error / checks[].detail / 8-char partial (step 2) — all green. `user@host` (no password) stripped. pg prose 3 phrasings redacted (`for user "x"`, `role "x" does not exist`, `user 'x' has no permission`). `redactions` reports `url_credentials` + `db_username` (hits threaded through safeQuery/collectDatabase/buildBundle). Negative controls: plain errors intact; scrubText no-op returns input.

| mutant | probe red | dev red |
|---|---|---|
| M5 shrink PG_USER_PHRASE to `for user` only | 3 | 0 — dev suite lacks a `role "x"` case through buildBundle |
| M6 drop URL_CREDENTIALS strip in scrubText | 13 | 11 |
| M7 swallow hits | 2 | 2 |
| M8 drop stripUrlCredentials from connection line | 1 | 0 — only the non-parsable full-mask case; URL_CREDENTIALS now overlaps |

OBSERVED (pre-existing, made visible by #94): 14-digit migration ids (`20260902100000`) redacted as `credit_card` in remedy text and migration_name rows; `redactions` shows credit_card on a clean install. Same pattern family as #100/#101.
