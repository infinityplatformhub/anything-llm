# QA-2 — #131 `217343aba` — PASS + 2 findings (recorded by PMO from QA-2 message body; QA-2 is read-only)

Probe 94/94; with dev suites 355/355. All assertions on "value gone", never on non-empty `redactions`.
12 codepoints × every interior position × 4 classes: gone. `vic<ZWSP>tim@`: no vic/tim/victim remains. Thai + U+200B no-hit: byte-identical, 5 joiner segments. O5b scrubText: all 12 pass, Thai untouched.

FINDING-1: strip is whole-string on hit. Field with Thai + one PII loses every ICU joiner (4→0) for every class. Redaction correct; adjacent Thai collapses to one run. Options: per-match strip (needs offsets) or accept as residual with an honest comment. Pinned as tests both ways.

FINDING-2: still leaking — U+FE00, U+FE0F (Mn), U+3164, U+115F, U+FFA0 (Lo Hangul fillers), U+17B4 (Mn), U+2800 (So). First six are `\p{Default_Ignorable_Code_Point}`; 11/12 listed codepoints are DICP too. `[\p{Cf}\p{Default_Ignorable_Code_Point}͏￹]` closes six in one change. U+2800 is a decision, not a category.

Mutations: M1 remove strip 63/71; M2 Cf-only 5/7; M3 list-only 0→5 (probe and dev suites used only listed codepoints; scanned Unicode: 159 codepoints stripped by shipped code but not by list-only; added group G with U+0600/200F/2067/206A/1D173); M4 keep stripped on no-hit 3/3. Dev suite still lacks a Cf-outside-list case.
