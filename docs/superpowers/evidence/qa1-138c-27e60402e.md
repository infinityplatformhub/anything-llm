# QA-1 — #138 combined `27e60402e` — PASS (written by PMO from QA-1 body)

Fresh migrate+seed DB. 10 probes green:
```
rf1 rf2 rf2b rf2c m3 rf3c rf4 rf5 = green; rf6b = PASS (run separately)
```
**RF-6 now finds the real permission row** (`{action:"directory.sync", scope:"org"}`; on 4c01ac1ca it was null). Verified in DB: exactly one role holds it — `super_admin | allow`; `setup_admin` holds nothing on it, so the 403 side is real, not vacuous.

Unchanged from 4c01ac1ca: RF-2c (LeaseLostError, afterResume 6, live-lease control → 7); RF-3c (`directory.sync:lark-…` 160000 measured 160001; control `telemetry.flush:x-…` 30000); RF-6b (job id reused, one pending row, key `directory-sync-manual:ldap:<minute>` stable).

Mutants not re-run: diff vs 4c01ac1ca is the route-count pin + fixture upsert, neither touching the guarded code; red sets established distinct on 4c01ac1ca (M1→rf3c, M2→rf1, M3m→rf2, M4→rf4, M5→rf2c, M6→rf5).

Residuals (not blocking): RF-3d telemetry.flush claimed at 160001 vs own 30000 (JobRuntime Math.max, TL-1 holds ruling); `lark` absent from provider registry → sync-now 404 (#141).
