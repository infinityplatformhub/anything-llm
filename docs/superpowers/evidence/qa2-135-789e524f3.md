# QA-2 — #135 `789e524f3` — PASS (written by PMO from QA-2 body)

Skills: independent probe (QA-2 read-only oracle), node v22.23.1 pinned. 50 assertions: 45 green / 5 red; every red is a model-layer discriminator a correct fix leaves red.

## Correct-fix signature
| site | result |
|---|---|
| P6 session route | grants 0, acl 0, revocations 1 — GREEN |
| P7 developer API | grants 0, acl 0, revocations 1 — GREEN |
| P7 control (weak key) | 403, victim alive, grants intact — GREEN |
| P8 site 3 truncate | grants 9→0, acl 6→0, versions 46→47 — GREEN (new) |
| P4/P5 model layer | red BY DESIGN — cleanup did not go into `User.delete` |

## Admin-route refusal shape
setup_admin deleting super_admin → `200 {"success":false,"error":"Cannot perform that action on user."}`, victim alive, grants 1→1. Real refusal, different shape. Oracle corrected from `status !== 200` to outcome: refused = 403/401 or 200+success:false, plus victim survives and grants intact.

## P8 (new)
Site 3 cleanup lives in system.js catch as `truncateUserPrincipalAuthorization`; `User.delete` untouched so P4 is blind. P8 drives the exported function: grants/ACLs to zero, exactly one version bump, non-user principals untouched. No HTTP fixture for site 3 (runs inside a catch on a failing settings write).

## Mutants
- W1 truncate drops version bump — CAUGHT
- W2 truncate deletes non-user principals — SURVIVED first (`groupGrants >= 0 && true` tautology), rewritten to count-before/after equal AND non-zero — now CAUGHT

Lesson: an assertion containing `&& true` is not an assertion.
