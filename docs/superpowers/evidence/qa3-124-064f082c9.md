# QA-3 — #124 re-probe `064f082c9` (Dev4, a11y, tier `plain`)

**Verdict: PASS.** The surviving mutant from `4646ed17b` is dead. Production code is
unchanged between the two SHAs — only the test file (+38/−11) and the ledger moved, which
is the right shape for a coverage fix.

Worktree `/tmp/qa3-104` @ `064f082c9`. Node 22, `frontend/`, vitest.

## Baseline

`yarn test` → **exit 0**, 15 files / 96 tests (exit code checked, not just counts).

## Mutants — same three, same script, re-run

Applied by the same quoted-heredoc `.py` used on `4646ed17b`, literal anchor, applied
`git diff` printed before each run, full `yarn test` each time.

| # | mutation | `4646ed17b` | `064f082c9` |
|---|---|---|---|
| a | `aria-label={modelName}` | killed (1 failed) | **killed** — exit 1, 3 failed |
| b | **delete the `aria-label` line** | **SURVIVED**, exit 0 | **killed** — exit 1, 3 failed |
| c | `aria-label="Workspace"` | killed (3 failed) | **killed** — exit 1, 3 failed |

All three now die in the same three tests (reachable-by-name, name-does-not-change,
no-model-set). Dev4's report of "(b) red on 3" matches what I measured independently.

## Why (b) now dies — both holes closed, checked separately

- **The fallback window.** Tests 1 and 4 now wait for the resolved state
  (`waitFor(getByText(/gpt-4o-mini/))`) before querying, and assert
  `button.getAttribute("aria-label")` matches directly rather than relying on
  `getByRole({name})` — which was matching the *visible text* during the pre-load window.
  Test 4 has no model by construction, so it asserts the attribute instead of the name;
  that is the correct handling for that state, not a workaround.
- **`null === null`.** Test 2 adds `expect(firstName).toEqual(expect.any(String))` and
  `expect(firstName.length).toBeGreaterThan(0)` before the equality, so a missing
  attribute no longer satisfies the constancy claim. Equality alone was never the
  property; a stable *name* is.

## One extra mutant, not asked for

| d | `aria-label={modelName ? t("chat_window.select_model") : undefined}` — labelled only when a model is set | **killed** — exit 1, 1 failed (`with NO model set…`) |

This is the "name that moves in a quieter form" the test-4 comment claims to guard. It
does. Test 4 is load-bearing, not decorative.

## Header claim

The false RED claim from `4646ed17b` is removed and replaced with an accurate account of
why it was false, including the two rules derived from it. The file now describes what it
actually proves.

## Housekeeping

All mutations reverted; `git status --porcelain` clean in the worktree. No commits.

Supersedes `qa3-124-4646ed17b.md` (FAIL).
