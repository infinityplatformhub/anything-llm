# QA-3 evidence — #61 V9 54dcdc34 — PASS (two UTF-8 DBs + one C-locale DB, real route stack)
Author: QA-3 (anything-llm-ea), transcribed by PMO.
F1/F2/F3 closed: assistant edit, prompt edit, thread update-chat, upsert create+update, bulkCreate all derive response_text (old word 0 hits, new word 1 hit). F4: natural plan at 20k rows is Seq Scan (correct); per-column with SET LOCAL enable_seqscan=off inside $transaction → Bitmap Index Scan on both trgm indexes, Thai 0.344ms / English 0.478ms. F5: migration RAISE WARNING on ctype=C (psql; prisma CLI does not echo it — boot layer necessary), boot error on C / silent on UTF-8, array_length NULL trap handled (mutant → 2 fail).
Mutations 5/5 caught (user_id 4, _update 3, upsert 1, bulkCreate 2, locale NULL 2). Source-scan test has guard-the-guard; all 10 prisma writes live in the model, 4 touch response via helper. Suites green on both locales (36/36, 31/31). P1–P8 unchanged. Timing UTF-8 20k rows: cold 615ms then 1–2ms.
Nit (pre-existing): `WorkspaceChats.upsert` returns `{chat: undefined}` (destructures a bare row); sole caller ignores it.
