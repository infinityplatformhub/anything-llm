# QA-1 evidence — #48 66cb5e7d — PASS (BLOCKER-1 closed)

Author: QA-1 (anything-llm-af), transcribed by PMO. Own detached worktree, gate 1387/1391 (2 red = #47/#57 flakes, solo green).

E-set (operator flow): login → update-password → AUTH_TOKEN + JWT_SECRET rows stored → unauth update-env 401 → `DELETE /system/credential/AUTH_TOKEN` **400** "is instance authentication, not a provider credential; use /system/update-password" → env untouched, row intact → unauth update-env **still 401** (decisive).
J-set: JWT_SECRET same denylist 400; env before===after; row intact; existing session token still valid. (JWT rotation seen in E1 is update-password's own behaviour, not DELETE.)
Denylist with rows seeded: SIG_KEY / SIG_SALT / API_KEY_PEPPER → 400 denylist reason (previously "not a stored credential" = allowlist fallthrough).
Coverage: 91 secret:true keys; passthrough env names in validatedRequest/validApiKey/validBrowserExtensionApiKey/deploymentMode/simpleSSOEnabled = [AUTH_TOKEN, JWT_SECRET]; clearable-and-auth-breaking = [].
Positive control: OPEN_AI_KEY clear 200, stays unset after loadStoredCredentials.
NIT-1: AUTH_TOKEN unset → 401 [003] both empty and arbitrary password; audit rows carry no password.
Mutations (baseline 62/62): M7 clear denylist 2 fail, M8 drop AUTH_TOKEN 1, M9 drop pattern 4, M1 #41 join 2, M6 ALLOWED_KEYS 1 — all caught. **M10 remove 401 guard → survived.**

## NIT-1 (non-blocking, → #59 branch)
401 guard has no test (M10). Missing `password` field → `bcrypt.compareSync(undefined, hash)` throws → 500, with AUTH_TOKEN set or unset (pre-existing on main). One-line fix: `if (!process.env.AUTH_TOKEN || typeof password !== "string")` + test.
