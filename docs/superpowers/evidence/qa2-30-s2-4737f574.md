# QA-2 — #30 slice 2 `4737f574` — **PASS** (lane: /v1 + router + embed)

Probe 15/15 · mutation 6/6 caught · ไม่รัน full suite (§7.14) · (เนื้อหาส่งโดย QA-2 anything-llm-e6 read-only, PMO commit)

## ขอบเขต
lane นี้คือ call site ฝั่ง `/v1`, model router และ embed. lane real-store 5 dialect เป็นของ QA-1.

## ผลตามเกณฑ์
**`/v1/openai/chat/completions` ไม่รั่ว** — alice สมาชิก workspace A เท่านั้น ยิง `model: "<slug ของ B>"`: sync branch / stream branch / citations (`PINNED_SECRET_OF_B`, `b-secret`) → ไม่มีทั้งหมด · positive control: workspace ตัวเอง → 200 + `PINNED_DOC_OF_A` ใน prompt จริง

**tenancy จาก `document.orgId`** — doc org 2 ใน workspace ที่อ่านได้ ถูกตัด รวม branch `orgWide`
**scope เชิงบวก** — workspace นอก `workspaceIds` → `[]`; control `orgWide` ยังเข้าถึง workspace ที่ไม่ได้ list
**embed** — `actorRef` ถึง `authorizedPinnedDocs` ผ่าน `gatherRoutingContext` และ `streamChatWithForEmbed`; ไม่มี principal → fail-closed

## positive control 403 (สำคัญ)
รอบแรก `/v1` ตอบ 403 → assertion ผ่านแบบว่างเปล่า ไม่รายงาน PASS · สาเหตุ: `/v1/openai/chat/completions` ไม่ประกาศ workspace binding → `addressedWorkspaceId` undefined → engine authorize ด้วย `workspaceId: null` → grant scope `workspace` ไม่ match · แก้ด้วย role scope `org` ถือ `chat.write` grant `workspaceId: null` — ทำให้ probe แข็งขึ้น (ingress ปล่อย, การปฏิเสธมาจาก retrieval filter เอง)

## Mutation 6/6
| mutant | ผล |
|---|---|
| ปิด positive scope check | CAUGHT จากเทส `/v1` HTTP โดยตรง |
| scope รับทุก workspace เมื่อ orgWide=false | CAUGHT |
| ถอด org/tenancy check | CAUGHT |
| unreadable org admit แทน unprovable (M4) | CAUGHT หลังเพิ่มเทสสองทิศ |
| router ตัด `actorRef` | CAUGHT |
| embed ตัด `actorRef` | CAUGHT |

### M4
ถอด branch `rowOrgId == null` รอดรอบแรก: `String(undefined) !== String(1)` ตัดแถวอยู่ดี — fail-closed ทั้งสองทาง ไม่ใช่รู · ต่างกันเฉพาะตอนเปิด `RETRIEVAL_FILTER_ALLOW_UNPROVABLE` (unprovable ต้อง admit ไม่ใช่ตัดเงียบเป็น mismatch) · เพิ่มเทส flag on → admit / off → excluded แล้ว caught

## บั๊ก probe (แก้แล้ว)
1. ยิง `/v1/...` แทน `/api/v1/...` → ค้าง timeout (§7.15) 2. timeout 5s → 30s 3. `ApiKey.create` ต้อง `key.manage` → เขียน `api_keys` ตรง hash ด้วย `digestSecret` จริง 4. `resolveLLMConnectorForEmbed` ไม่ export → ขับ `streamChatWithForEmbed`

## Finding ค้าง (ไม่ใช่ของ slice นี้)
embed principal ไม่เคยได้ role grant → `buildDocumentFilter` match-none ทุก embed → availability bug (#69) มีมาก่อน slice 2 · ทำให้ content assertion ฝั่ง embed ว่าง จึง assert ที่การเดินทางของ actorRef
