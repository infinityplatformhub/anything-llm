# QA-1 #41 review — 674d8f19 (2026-09-02) — PASS
NFC request on NFD-stored file → 200 own, 404 other tenant. addToWorkspaces=<slug B> → 403 on raw-text/upload-link, 0 rows in B; validateWorkspaceSlugQuery on all 4 routes. 404 body byte-identical mismatch vs not-found. attachTargets: bound → own slug, deleted workspace / db throw → {slugs:"", error}, unbound untouched; routes 500 success:false. boundDocpaths throw → empty set → bound sees nothing. 1250/1250. Mutation M2 4 fail, M3 1 fail, M1 (join throw → null) 33 pass → NIT-1 test owed. NIT-2 readdirSync per resolve.
