# QA-2 — #80 `719b7eee` — FAIL (rate limit) / lane ที่เหลือ PASS

Probe 16/17 · mutation 7: caught 3, equivalent 4 · เขียนโดย PMO จากข้อความ QA-2

## FAIL: `inviteMailRateLimit` ไม่ถูก mount
grep ทั้ง server = นิยาม `requestControls.js:239` + export `:317` เท่านั้น · `endpoints/admin.js` มี `RateLimit` 0 · ยิงจริง `INVITE_MAIL_RATE_LIMIT_MAX=2` ส่ง 4 ครั้ง → ไม่มี 429 · `mailerTestRateLimit` สภาพเดียวกัน · inert guard (ตระกูล slice 1b flag / bypass 8) — mutation จับไม่ได้ ต้อง assert เชิงโครงสร้าง · เทส `requestControlsHttp` ยิง app สังเคราะห์ด้วย `inviteRateLimit` ตัวเก่า ไม่แตะ route จริง

## ผ่าน (16)
- `/v1` + email → 400, 0 message, 0 invite row; array 400; `""` = copy-link 200
- decode quoted-printable ก่อน grep `event_logs` (code + address) + CONTROL code อยู่บนสาย
- RCPT TO ตัวเดียว · password ไม่อยู่ในบอดี้
- invite.create ไม่มี user.manage → 403 ไม่มีเมล + control copy-link ได้ · channel ปิด → 4xx ไม่สร้าง invite · array ที่ admin route → 400

## Mutation
caught: `/v1` เลิกปฏิเสธ email (4) · ถอด `user.manage` (2) · ข้าม `assertChannelReady` (2)
equivalent: array (regex ADDRESS ปฏิเสธ comma) · `""` (falsy path เดิม) · audit เก็บ code (value scrubber จับ `apw-inv-` pattern) · audit เก็บ address (`email` ไม่อยู่ใน allowlist)

## Dependency ที่มองไม่เห็น (→ ruling ขั้น 6)
การกัน invite code ใน audit ขึ้นกับ **รูปแบบ** `apw-inv-<base64url>` ไม่ใช่ allowlist — `inviteId` รับ UUID สังเคราะห์ผ่านทะลุ · ต้อง pin: รูป `apw-inv-` ถูก scrub, `models/invite.js` ยังใช้ prefix นี้
