# QA-2 — #94 cefab9864 — FAIL (2 findings)

Probe 38 cases: 35 green, 3 red (`qa2probe94.test.js`, kept at /tmp/qa2-probe94.js). Dev suite green on this SHA.

**FINDING-1** (confirms TL-1): password leaks in full via `safeQuery` error when host has no dot. `@db.internal.corp` clean (email pattern), `@postgres` / `@localhost` leak: `database.migrations.error: "connection failed for postgresql://appuser:PLAINPASS123@postgres:5432/anythingllm"`. `stripUrlCredentials` in `collectEnv` is load-bearing (mutation M3: raw URL passes scrub).

**FINDING-2** (new): bundle carries DB **username** + internal host via `checks[].detail` (doctor `maskUrl` keeps user: `Connected to postgresql://qa2_leak_probe:****@localhost:55451/qa`) and via pg failure text `password authentication failed for user "appuser"`. `database.connection` itself is clean. Username/hostname match no redaction pattern.

Mutations: M1 OPEN_AI_KEY into DERIVED → 5/2 red; M2 drop scrubValue in buildBundle → 6/3; M3 drop strip in collectEnv → 3/3; M4 drop event-name scrub → 2/1.

Checks 1–6: stdout JSON whole / checklist stderr / exit = checks ✓; 8 seed classes + 8-char sliding window (red at F1); connection clean; resources all numbers; allowlist holds under 214 envKeys set at once; order swap = M3.

Minor: `collectDatabase` defaults to `process.env.DATABASE_URL`, ignoring injected `env` — seam only.
