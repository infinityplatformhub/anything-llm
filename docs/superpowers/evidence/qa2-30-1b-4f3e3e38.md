# QA-2 evidence — #30 slice 1b 4f3e3e38 — FAIL (qdrant escape clause inert); 69/70, suite 152/152 1680/1680 with real Milvus

Author: QA-2 (anything-llm-e6), transcribed by PMO. Own worktree (yarn install), real qdrant v1.7.4 + Milvus (Dev4's t5-milvus:19531).

## BLOCKER — `vectorPredicate.js:332` toQdrantFilter uses `is_null`
qdrant `is_null` matches only explicit JSON null, not absent keys; pre-T-5 vectors have absent keys → flag ON still hides them = inert (same class as 1a's two fails). isRowAllowed treats undefined==null; pushdown does not.
```
all              : absent, explicitnull, foreign, halfA, mine
shipped is_null  : explicitnull, mine
fixed   is_empty : absent, explicitnull, mine   (foreign/halfA still excluded)
```
Fix: `{ should: [{ must: ACL_FIELDS.map(key => ({ is_empty: { key } })) }, filter] }`. RED test must write a point with the key ABSENT (a null-valued fixture stays green while broken). pinecone/astra `$exists:false` unaffected.

## Pass
Milvus 9/9 (escape parses; unparenthesised form rejected by server = RED proof); qdrant 8/9; chroma 5/5 (predicate == strict both states, error-level boot log, qdrant not misflagged); dialect table 6/6 (no empty filter, deny beats allow, weaviate closed enum); write path 4/4 (8 providers, ACL spread last, missing field throws); 42P01 3/3 (0/0 on fresh, narrow catch); regression 29/29 on Lance + pgvector.

Verdict: FAIL — one fix + absent-key RED test.
