# QA-1 evidence — #58 953e108a — PASS (3 residuals, none blocking)

Author: QA-1 (anything-llm-af), transcribed verbatim by PMO (QA is read-only in repo).
Worktree h58 clean at 953e108a. Gate 1246/1250 (2 red suites = known flakes cacheIsolation #51 + providerDocIdCallSites #57; solo 23/23 green). New tests 3 files 11/11. Probe DB `qa1_58`, dropped.

## /request-token runtime guard — PASS
```
P2 genuine single-user (false, 0 users), AUTH_TOKEN  -> 200 token=issued
   insert user row while process still running (no restart)
P1 shape(b) AT RUNTIME, correct AUTH_TOKEN            -> 401 token=null
P1b shape(b) wrong password                           -> 401
P1c body byte-identical both cases                    -> true (no oracle)
P3b delete user row, AUTH_TOKEN                       -> 200 token=issued
```
Truly runtime: `User.count()` per request, deny/allow flips both ways without restart.

## restart (real bootHTTP) — PASS
```
R0 pre-boot: setting=false users=1
R1 post-boot setting=true (repair before listen)
R2 after restart AUTH_TOKEN -> 401
R4 normal multi-user login  -> 200 token=issued
```

## boot repair — PASS
```
B1 (false, 0 users) -> repaired:false consistent, setting untouched
B2 (true, users)    -> repaired:false consistent
B3 shape(b)         -> repaired:true setting=true
B3 log leak check: AUTH_TOKEN/DATABASE_URL/API_KEY_PEPPER/JWT_SECRET/username = false
B4 repeated boot in shape(b) -> logs every time
B5 MODE_REPAIR_ACKNOWLEDGED=1 -> still repairs, silent
B6 DB outage -> reason:"unreadable", no write, no relabel
```

## mutation (baseline 11/11)
```
M1 remove /request-token runtime guard     -> 1 failed  ok
M2 repair never repairs                    -> 2 failed  ok
M3 repair always (breaks true single-user) -> 2 failed  ok
M4 revert ruling B (browser ext key)       -> 2 failed  ok
M6 revert mobile validRegistrationToken    -> 1 failed  ok
M5 revert ruling A (validApiKey)           -> 11/11 SURVIVED
M7 revert isMultiUserSetup                 -> 11/11 SURVIVED
```

## Residuals (not blocking)
- **NIT-1** ruling A (`validApiKey.js:111`) has no test; M5 survives. Code verified correct by probe (shape(b) → `locals.multiUserMode=true`). M7 same, but fail-closed. [→ residual-risks]
- **NIT-2** `assertDeploymentShape.js:44` trusts the write. `_updateSettings` returns `{success:false,error}` instead of throwing (systemSettings.js:743). Probe: write failure → `{repaired:true}` + "[DEPLOYMENT SHAPE REPAIRED]" while setting stays false. Not a security hole (4 fixed sites use `isConfirmedSingleUser()`, computed live). Same non-throw breaks `enable-multi-user` (system.js:748) catch{} rollback → shape(b) reachable without SIGKILL. Same as Techlead NIT-1. [→ follow-up issue]
- **NIT-3** ruling D site `websocket.js:17` confirmed open: D1 `userCanToggleTools(default user)` in shape(b) → true; D2 same user true multi-user → false. Deferred per ruling D. [→ residual-risks]
