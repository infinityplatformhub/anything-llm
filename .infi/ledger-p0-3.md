Ruling: Reuse Jest 29 and add Supertest only — root already standardizes Jest but server lacked direct runner — cost if wrong: duplicated Jest dependency and lockfile weight
Ruling: Export existing Express app and suppress listener when imported — Supertest should exercise production route registration without opening a port — cost if wrong: index.js consumers could depend on import side effects
Ruling: Allow DATABASE_URL Prisma constructor override while retaining sqlite default — regression tests need isolated temporary databases without changing current deployment schema — cost if wrong: malformed deployment override can select unintended database
Ruling: Test current lockout absence rather than invent lockout — P0-4 owns login rate limiting and P0-3 must freeze current behavior — cost if wrong: future hardening intentionally breaks this assertion and requires suite update
Ruling: Split PR CI from tag release workflow — PR gate stays fast while release can build and publish image — cost if wrong: image build regressions appear only at tag time
Ruling: RED proof used three temporary production breaks restored before commit — inverted API-key acceptance, removed workspace assignment filter, bypassed role check each failed its targeted test — cost if wrong: tests could pass without guarding claimed regressions
Ruling: PMO scope-approved `server/index.js` app export and listener guard plus test-only Prisma datasource override — standard Supertest app import and isolated sqlite require these minimal production changes; DATABASE_URL is now ignored unless NODE_ENV=test — cost if wrong: import behavior or production datasource selection could change
Ruling: Pin local and CI verification to Node.js 22 using `.nvmrc` — Node 26 breaks upstream jsonwebtoken transitive `buffer-equal-constant-time`; supported Node 22 gives reproducible suite — cost if wrong: contributors on unpinned Node can see dependency failures unrelated to application behavior
Ruling: Upload-query regression stubs only external collector HTTP response, vector engine, and final LLM completion — real upload middleware, CollectorApi, Document.api.uploadToWorkspace, Document.addDocuments, sqlite persistence, ApiChatHandler.chatSync, and query route carry uploaded content end-to-end — cost if wrong: fixture contract can diverge from real collector/vector response shape
Ruling: RED proof 1 API key — mutated `server/utils/middleware/validApiKey.js:17` from `if (!(await ApiKey.get({ secret: bearerKey })))` to `if (await ApiKey.get({ secret: bearerKey }))`; ran `cd server && PATH=/opt/homebrew/opt/node@22/bin:$PATH yarn test --runInBand __tests__/api/regression.test.js -t 'accepts valid API key' --forceExit`; test `API key auth › accepts valid API key` failed with `Expected authenticated: true / Received error: No valid api key found.`; restored original file and verified clean diff — cost if wrong: valid API-key regression could escape
Ruling: RED proof 2 workspace isolation — mutated `server/endpoints/workspaces.js:382-384` from multi-user `Workspace.whereWithUser(user)` branch to unconditional `Workspace.where()`; ran `cd server && PATH=/opt/homebrew/opt/node@22/bin:$PATH yarn test --runInBand __tests__/api/regression.test.js -t 'member list omits unassigned workspace' --forceExit`; test `workspace isolation › member list omits unassigned workspace` failed with `Expected value: not "hidden" / Received array: ["assigned", "hidden"]`; restored original file and verified clean diff — cost if wrong: unassigned workspace leak could escape
Ruling: RED proof 3 role gate — mutated `server/utils/middleware/multiUserProtected.js:44` from `if (allowedRoles.includes(user?.role))` to `if (true || allowedRoles.includes(user?.role))`; ran `cd server && PATH=/opt/homebrew/opt/node@22/bin:$PATH yarn test --runInBand __tests__/api/regression.test.js -t 'default user gets 401 on get /api/admin/users' --forceExit`; test `role gates › default user gets 401 on get /api/admin/users` failed with `Expected: 401 / Received: 200`; restored original file and verified clean diff — cost if wrong: default-user admin access could escape
Ruling: Node 22 green evidence — command `cd server && PATH=/opt/homebrew/opt/node@22/bin:$PATH npm exec --yes yarn@1.22.22 -- test --forceExit`; observed tail: `Test Suites: 45 passed, 45 total`, `Tests: 617 passed, 617 total`, `Snapshots: 0 total`, `Time: 4.75 s`, `Ran all test suites.` — cost if wrong: release gate lacks reproducible local baseline
Ruling: Close Jest handle at source by unref-ing `EmbeddingWorkerManager.emitProgress` 10-second replay-cache cleanup timer — `--detectOpenHandles` first identified model-map remote fetch, fixed in API test by mocking model map; exact full contract still warned for 10 seconds, matching this timer created by upload integration; unref preserves cache cleanup while allowing process exit — cost if wrong: process may exit before in-memory replay cache cleanup, harmless because process teardown discards cache
Ruling: Open-handle green evidence — exact contract `cd server && PATH=/opt/homebrew/opt/node@22/bin:$PATH yarn test 2>&1 | tail -5` now outputs `Tests: 617 passed, 617 total`, `Snapshots: 0 total`, `Time: 4.606 s`, `Ran all test suites.`, `Done in 5.39s.` without Jest warning — cost if wrong: task.sh evidence tail loses required `Tests:` again
