CONTRACT_CMD=sh -c 'cd server && npx jest __tests__/api/persistCredentialFailureHttp.test.js 2>&1 | grep -E "Tests:" | grep -v failed; exit 0'
CONTRACT_EXPECT=passed
MOCKUP_PATH=
MOCKUP_SHA=
TASK_TYPE=bug
