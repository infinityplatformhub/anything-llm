Ruling: Give each HTTP suite a process-scoped PostgreSQL schema through DATABASE_URL — matches production provider while keeping suites isolated and preserving test-only Prisma override behavior — cost if wrong: interrupted suites can leave disposable schemas until database cleanup
Ruling: Keep assertions and HTTP request paths unchanged — hotfix repairs unavailable database setup without weakening SSO lock or env-dump authorization proof — cost if wrong: future failures still point to security behavior rather than harness churn
Ruling: Regression must run after integration on current main, not only each branch against its older base — both source branches passed independently while their merge made SQLite test harnesses incompatible with PostgreSQL schema — cost if wrong: cross-branch incompatibilities remain invisible until later QA
Ruling: Root package.json needs no change — Prisma dependencies already live in server/package.json and working tree has no pending root package diff — cost if wrong: root-level tooling that directly imports Prisma would need explicit dependencies later
