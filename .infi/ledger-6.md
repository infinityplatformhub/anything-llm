Ruling: Never enable Express trust proxy globally; derive enforcement identity only from request.socket.remoteAddress — forwarded headers are attacker-controlled without an explicit trusted proxy topology — cost if wrong: deployments behind a proxy share its socket bucket until operators add a specific hop/CIDR policy
Ruling: Combine per-IP login limit with per-(socket-IP, normalized username) limit and a short fixed window — prevents one remote attacker locking a victim globally while still throttling credential stuffing — cost if wrong: distributed attacks across many source addresses need a shared adaptive defense later
Ruling: Normalize usernames with trim and lowercase before HMAC bucket derivation — login identity lookup semantics must not permit casing or whitespace to split limiter buckets — cost if wrong: a future case-sensitive username policy needs matching limiter normalization
Ruling: HMAC all bearer and login bucket material and cap each in-memory store at 10000 attacker-selected keys — limiter memory must not retain raw credentials or grow without bound — cost if wrong: high-cardinality legitimate traffic needs a shared bounded store and measured capacity
Ruling: Keep trust proxy unset and treat X-Forwarded-For, X-Real-IP, Forwarded, CF-Connecting-IP, and True-Client-IP as inert — one untrusted header must never bypass rate limits or IP zoning — cost if wrong: proxy deployments must configure network-level source preservation or a narrowly trusted proxy policy
Ruling: Enforce a deployment IP/CIDR allowlist on /api using socket address, with empty config allowing traffic and malformed config failing closed — default-off avoids bricking upgrades while invalid security configuration cannot silently allow — cost if wrong: malformed config returns 403 until corrected
Ruling: Return byte-identical responses for missing and non-pending invite codes, and return one 401 response for unknown-user and wrong-password login — public responses must not reveal account or invite validity — cost if wrong: clients relying on old diagnostic strings need updates

Ruling: Treat suspended accounts exactly like unknown users and wrong passwords with the same 401 body — distinct status or message confirms account existence — cost if wrong: suspended users lose a specific login diagnostic and need an out-of-band support path
Ruling: Accept same-IP username lockout within the short window as a single-node trade-off — per-(IP,username) prevents remote global victim lockout but a coworker behind the same NAT can still lock that tuple; evaluate exponential backoff in P0-5 — cost if wrong: hostile shared networks can deny one account login for the configured window
Ruling: Fail closed into one shared overflow bucket after 10000 limiter keys — bounded memory is safer than attacker-driven growth, but new clients can share throttling once saturated; replace with a shared bounded store before horizontal scaling — cost if wrong: saturation causes temporary collateral throttling for previously unseen keys
