Ruling: Centralize test DATABASE_URL derivation in server/utils/test/postgresUrl.js — every PostgreSQL integration suite must use the same parser instead of regex or ad hoc searchParams edits — cost if wrong: production-like query parameters can break only selected suites again
Ruling: Strip Prisma-only schema/connection_limit and libpq sslmode parameters from direct node-postgres test clients, and strip all query parameters for psql — test harness clients do not share Prisma's URL dialect and local PostgreSQL fixtures may not support SSL — cost if wrong: a test that specifically validates SSL must construct its own explicit SSL client configuration
Ruling: Remove inherited connection_limit from isolated Prisma test URLs while preserving only the derived schema — runInBand still hosts multiple Prisma clients and a production pool cap can starve setup hooks — cost if wrong: tests no longer exercise deployment pool sizing, which belongs in a dedicated pool integration test
Ruling: Give database setup and cleanup hooks 30 seconds — schema push and import are correctness checks, not latency assertions, and cold PostgreSQL/Prisma startup can exceed Jest's 5-second default — cost if wrong: a genuinely stuck setup takes longer to report
