# Recon P0-1: Modular architecture contracts (F6)
- โค้ดเดิม modular แล้วครึ่งเดียว: server/utils/AiProviders/ (~30 LLM), EmbeddingEngines/, vectorDbProviders/ — เป็น directory-per-provider convention
- ที่ยังฝัง: auth ใน endpoints/system.js, permission กระจาย ~23 ไฟล์ผ่าน flexUserRoleValid, chat pipeline เป็นก้อนใน utils/chats/, ไม่มี job queue/event bus (EventLogs.logEvent เรียกตรงจาก call-site)
- งาน: เขียน contract 11 ไฟล์ (8 seams + queue + bus + storage) ลง docs/superpowers/design/seams/ + sequence diagram >=10 use cases
- Plan: docs/superpowers/plans/phase0-foundation.md (P0-1) @ 39049ab7
