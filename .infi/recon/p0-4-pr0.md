# Recon P0-4 PR-0: SSO issue-auth-token hotfix
- ช่องโหว่: simpleSSOEnabled (server/utils/middleware/simpleSSOEnabled.js:11-34) เช็คแค่ env + multi-user mode ไม่เช็คว่า key ไหนเรียก → API key ใดก็ได้ขอ temp token ของ user ใดก็ได้รวม admin ที่ GET /v1/users/:id/issue-auth-token (userManagement/index.js:68) แล้วแลก session JWT ที่ system.js:353 = full impersonation
- งาน: ปิด endpoint ด้วย env flag default-closed (403 + เหตุผล) จนกว่า scope lock (PR-5) พร้อม; exchange endpoint คงไว้ (token เดิม 6 นาที)
