# Recon P0-4 PR-1: Key generator entropy
- ปัจจุบัน uuid-apikey (server/models/apiKeys.js:7, browserExtensionApiKey.js:10) = Base32 ของ UUIDv4 = 122-bit
- งาน: crypto.randomBytes(32) base64url, คง prefix brx- (browserExtensionApiKey.js:41 เช็ค prefix); ยังเก็บ plaintext จน PR-3 มา digest
- extension ไม่ hardcode format (grep brx ใน browser-extension/src = 0) → forced rotation พอ ไม่ต้อง dual-format
