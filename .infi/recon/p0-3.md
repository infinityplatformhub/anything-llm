# Recon P0-3: Test suite + release pipeline (F4)
- ของเดิม: server มี jest ใน devDependencies บางส่วน, upstream CI ไม่ได้ fork มาใช้ต่อ, fork ยาว = QA upstream หาย
- งาน: API regression suite ~30 เคส (supertest บน endpoint หลัก: auth, role gate, workspace isolation, upload, api key) + GitHub Actions PR gate + release tag -> image
- Plan: docs/superpowers/plans/phase0-foundation.md (P0-3) @ 39049ab7
