# Recon P0-4 PR-3: API key schema + digest + scopes
- Spec: docs/superpowers/design/p0-4-security-recon.md @ 8b49f5c3 (Step 0-2) + code-standards §6.1 (prefix apw-key-)
- ปัจจุบัน: api_keys.secret + browser_extension_api_keys.key เก็บ plaintext, validApiKey แค่ ApiKey.get({secret}) truthy → next(), ไม่มี scope/identity/expiry/last_used
- งาน (PR-3 ตาม task split ของ Dev 3):
  1. Migration ทั้ง 2 ตาราง: secretDigest (HMAC-SHA-256 + API_KEY_PEPPER), keyPrefix, scopes, workspaceId?, expiresAt?, lastUsedAt?, revokedAt? · drop plaintext = forced rotation
  2. validApiKey + validBrowserExtensionApiKey → resolve เป็น Actor (seam 02) บน response.locals
  3. requireScope middleware (default deny) — PR-3 ให้ทุก route scope ชั่วคราว `*` เพื่อไม่ break, ไล่ scope จริงใน PR-4a/b
  4. pepper fail-closed at boot (R8) — ไม่มี env = process.exit ก่อน listen, ห้าม self-assign
  5. timingSafeEqual + length guard
  6. audit event ผ่าน P0-6 bus (R4) — event ห้ามพก key material
- Attack matrix 21 ข้อจาก QA-2 (ขอไฟล์จาก anything-llm-5f) ต้องผ่านทั้งหมด
- DoD: no plaintext ใน DB, key เก่าใช้ไม่ได้, O(1) lookup ที่ 100k แถว, grep no-translation-table, dump-without-pepper ใช้ไม่ได้
