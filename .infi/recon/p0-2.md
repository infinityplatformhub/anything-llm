# Recon P0-2: Postgres migration (F1)
- ปัจจุบัน: prisma sqlite (server/prisma/schema.prisma:13-16), migrations เขียนแบบ sqlite, raw SQL บางจุด (queryRaw)
- Contract อ้างอิง: docs/superpowers/design/seams/11-storage.md + 09-job-queue (เลือก pg-boss-compatible substrate)
- งาน: provider postgresql + DATABASE_URL, migrations ใหม่ทั้งชุด, docker-compose เพิ่ม postgres:16 + healthcheck, สคริปต์ import sqlite→pg, ไล่ raw SQL dialect
- Plan: docs/superpowers/plans/phase0-foundation.md (P0-2) @ approof/main
