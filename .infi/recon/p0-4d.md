# Recon P0-4D(a): .env credential hygiene
- dumpENV (server/utils/helpers/updateENV.js ~:1473) เขียนไฟล์ .env ไม่ atomic ไม่ตั้ง 0600 — provider secrets จริงอยู่ในไฟล์นี้
- งาน: atomic write + chmod 0600 + owner check + secret-leak scan test (response/log/audit)
- ไม่แตะ schema → อิสระจาก #4/#5 · CredentialStore (D(c)) แยกไปหลัง #4
