# Recon P0-4A: Rate limiting + IP allowlist
- ที่มา: docs/superpowers/design/p0-4-security-recon.md Step 3+4 (Dev3 sub-issue A)
- ไม่มี rate limit ใด ๆ ใน repo, /request-token (server/endpoints/system.js:198) คืน 200 valid:false เมื่อรหัสผิด = brute force ฟรี
- งาน: express-rate-limit login per-IP+per-username + lockout + 429; general /v1 per-IP/per-key (HMAC bearer เป็น bucket key ไม่แตะ DB); IP/CIDR allowlist config-driven default off
- ไม่แตะ schema/event bus → ไม่บล็อกโดย #4/#5
