Ruling: P0-2 does not retain dual SQLite/PostgreSQL support — long fork and approved plan lock PostgreSQL as sole runtime database — cost if wrong: legacy SQLite-only deployments must migrate before upgrading
Ruling: Use Docker Compose PostgreSQL 16 for local tests and GitHub Actions service PostgreSQL 16 for CI — same substrate avoids SQLite semantic drift without adding testcontainer dependencies — cost if wrong: developers must have Docker running for full server tests
Ruling: Isolate regression and importer tests with per-process PostgreSQL schemas — Prisma client honors P0-3 test-only DATABASE_URL override while parallel machines avoid database collisions — cost if wrong: stale schemas can remain after interrupted tests
Ruling: Import legacy SQLite through sqlite3 CLI and pg parameterized inserts in one transaction — avoids native Node SQLite dependency while preserving IDs and retry-safe upserts — cost if wrong: migration host must provide sqlite3 CLI
Ruling: Import users, workspaces, workspace_users, workspace_threads, workspace_chats, workspace_documents, workspace_agent_invocations, workspace_suggested_messages, document_sync_queues/executions, slash_command_presets, prompt_history, api_keys, event_logs, and system_settings — preserves core identity, conversations, workspace content, agent history, sync state, prompt customizations, and audit/settings data while excluding caches, vectors, embeds, ephemeral auth/recovery tokens, device registrations, schedules, connectors, routers, and memories that are derived, security-sensitive, device-specific, or post-fork features — cost if wrong: an excluded non-derived legacy feature needs a separate export before upgrade
Ruling: Keep existing Int/serial primary keys in initial PostgreSQL migration — changing every key and foreign key to bigint exceeds approved provider migration scope and risks application compatibility — cost if wrong: high-volume tables need later online bigint migration before integer exhaustion
Ruling: Store every Prisma DateTime as PostgreSQL timestamptz(3) — timestamps represent moments and must preserve timezone semantics — cost if wrong: consumers expecting timezone-free wall times observe normalized UTC instants
Ruling: Bind the optional host PostgreSQL port to loopback — local tooling needs host access but network peers must not reach default development credentials — cost if wrong: remote developer hosts need an explicit secure tunnel instead of direct TCP
Ruling: Require @db.Timestamptz(3) on every Prisma DateTime field, including token expiries and sync scheduling — all values represent instants, and naive timestamps make expiry/scheduling depend on server timezone — cost if wrong: a future wall-clock-only field must document its exception and use a deliberate different type
